# برومبت التنفيذ — خلّي تطبيق الموبايل نسخة مطابقة للويب سايت

> **طريقة الاستخدام:** ابدأ بـ "التعليمات العامة" مرة واحدة في أول جلسة، وبعدين انسخ
> **فيز واحدة بس** في كل مرة، في محادثة جديدة لو حبيت. ماتديش الـ agent أكتر من فيز
> في نفس الوقت — بعضها بيغيّر سلوك أساسي في التطبيق ولازم يتراجع عليه قبل ما نكمل.
> الفيزات دي بنيت على مقارنة فعلية بين `mobile/client` و `app/` (مش تخمين) — كل نقطة
> فيها الملفات بالظبط اللي هتتلمس.

---

## ⚙️ التعليمات العامة (انسخها مع أول فيز، وكرّرها في بداية كل جلسة جديدة)

```
إنت Senior React Native / Expo Engineer شغال على مشروع "العاصمة" (Al Assema) —
منصة دليل خدمات للعاصمة الإدارية الجديدة.

## السياق
- الريبو: monorepo فيه `api/` (Next.js + Prisma)، `app/` (الويب سايت — Vite + React +
  React Router)، و `mobile/client` (تطبيق العميل — Expo / React Native، file-based
  routing بـ expo-router).
- شغلك كله في `mobile/client` بس. اقرا من `app/` عشان تفهم السلوك المطلوب تطابقه،
  بس **ماتلمسش أي ملف جوه `app/` ولا `api/`** إلا لو الفيز طلبت كده صراحةً.
- `mobile/client/AGENTS.md` بيقول: Expo اتغيّر كتير — اقرا التوثيق الرسمي المرتبط
  بالإصدار (v57) على https://docs.expo.dev/versions/v57.0.0/ قبل ما تستخدم أي API
  من Expo مش متأكد منه.
- التطبيق حاليًا **عربي بس، من غير أي دعم إنجليزي** (مفيش i18n ولا LocaleContext
  زي الموقع). ده خارج نطاق البرومبت ده تمامًا — سيبه زي ما هو، ماتضيفش دعم لغة
  تانية إلا لو مازن طلب كده في فيز منفصلة.

## قواعد ثابتة — ماتكسرهاش
1. **الرفع:** `npm run ship -- "وصف التعديل"` من جذر المشروع. بس. مفيش `rebase`،
   مفيش `push --force`، مفيش branches جديدة، مفيش merge — إلا لو مازن طلب صراحةً.
2. **قاعدة البيانات:** الفيزات دي كلها frontend جوه `mobile/client` — مالهاش أي
   علاقة بالداتا. ماتشغلش `seed` ولا `db:setup` ولا أي حاجة تلمس الداتابيز.
3. **أقفال git:** لو ظهر `index file corrupt` أو `index.lock`: `rm -f .git/index.lock`
   ثم `git reset`. بعد أي عملية git اعمل `git status` وتأكد إن النتيجة زي المتوقع.
4. **نطاق الفيز:** نفّذ اللي في الفيز الحالية بس. لو لقيت مشكلة تانية أو حاجة
   مش متأكد منها، سجّلها في قسم "ملاحظات" في نهاية شغلك وكمّل — ماتصلحهاش دلوقتي.
5. **قرارات المنتج/البيزنس — وقّف واسأل:** بعض الفيزات هنا (خصوصًا فيز 1) بتغيّر
   سلوك أساسي زي "هل التطبيق يسمح بتصفح من غير حساب؟". ده قرار منتج، مش باج.
   لو حسّيت إن أي تعديل هيغيّر قرار بيزنس، اعرض الخيارات على مازن ووقف لحد ما
   يرد، ماتفترضش وتنفّذ.
6. **حافظ على الكومنتات اللي بتشرح "ليه"** في الكود الحالي — ماتحذفهاش. لو نقلت
   كود، انقل الكومنت معاه.

## طريقة الشغل في كل فيز
1. اقرا قسم الفيز كامل، وافتح الملفات المذكورة فيها (في `mobile/client` و`app/`)
   بنفسك قبل ما تكتب أي كود.
2. اعمل خطة مختصرة بالملفات اللي هتتلمس/تتضاف، واعرضها عليّ قبل التنفيذ.
3. نفّذ.
4. شغّل `cd mobile/client && npx tsc --noEmit` — لازم يعدّي من غير أخطاء.
5. شغّل `npx expo start` واختبر يدويًا على الأقل على محاكي واحد (iOS أو Android)
   حسب "معايير القبول" بتاعة الفيز.
6. اعمل مقارنة جنب بعض (screenshot أو وصف) بين شاشة الموبايل وصفحة الويب المقابلة.
7. ارفع بـ `npm run ship -- "..."` بالرسالة المكتوبة في آخر الفيز.
```

---

## 📋 مرجع سريع — الفروقات اللي البرومبت ده بيعالجها

(النتيجة الكاملة لمقارنة `mobile/client` بـ `app/` — كل نقطة ليها فيز مقابلة تحت)

1. التطبيق بيجبر تسجيل دخول قبل أي تصفح؛ الموقع بيسمح بالتصفح كضيف.
2. مفيش لوجو ظاهر جوه أي شاشة في التطبيق (لا header ولا صفحة تسجيل الدخول)؛
   الموقع بيعرض اللوجو الفعلي (المرفوع من لوحة التحكم) في كل صفحة.
3. ترتيب وتشكيلة أزرار التاب بار السفلي مختلفة تمامًا عن أزرار الموقع.
4. صفحة "الشركات" في التطبيق فيها بحث بس، من غير فلاتر ولا ترتيب ولا صفحات.
5. صفحة بروفايل الشركة في التطبيق مبسّطة (مفيش section nav ولا سلة طلبات).
6. مفيش بحث شامل (global search) في التطبيق.
7. مفيش صفحة تستقبل لينك تأكيد الإيميل جوه التطبيق.

---

## 🔐 PHASE 1 — تصفح بدون تسجيل دخول (checkpoint إلزامي قبل التنفيذ)

```
## الهدف
دلوقتي `mobile/client/app/(tabs)/_layout.tsx` بيعمل Redirect لـ /sign-in لأي حد
مش عنده حساب، و`app/index.tsx` بيوديه على /sign-in برضه — يعني مفيش أي طريقة
تفتح التطبيق وتشوف حاجة من غيرها. الموقع (`app/src/RootLayout.tsx` +
`app/src/router.tsx`) بالعكس: أي حد يقدر يشوف الرئيسية/الخدمات/الشركات/بروفايل
الشركة من غير حساب، وبس لما يحاول يحفظ/يبعت طلب/يفتح حسابه بيتوجّه لصفحة
sign-in (شوف إزاي `/signin?next=...` شغالة في `app/src/pages/SignIn.tsx`).

## قبل ما تكتب أي كود
ده قرار منتج مش تفصيل UI. اعرض على مازن بالظبط:
- الوضع الحالي: لازم حساب من أول ثانية تفتح فيها التطبيق.
- المقترح: نفس منطق الموقع — الرئيسية/الخدمات/الشركات/بروفايل الشركة/عن
  العاصمة/تواصل معنا تتفتح من غير حساب، وبس "احفظ" / "ابعت طلب" / "طلباتي" /
  "الرسائل" / "حسابي" هي اللي تطلب تسجيل دخول (تفتح شاشة sign-in وترجّعه
  لنفس المكان بعد ما يسجّل، زي `next` بتاعة الموقع).
- استنى تأكيد صريح قبل ما تلمس أي كود.

## لو اتأكد، المهام
1. `app/index.tsx`: يوديك على `/home` دايمًا (مش على شرط `customer`).
2. `app/(tabs)/_layout.tsx`: شيل الـ `if (!customer) return <Redirect href="/sign-in" />`
   الكامل. بدّله بحماية على مستوى كل زرار/فعل بيحتاج حساب (Saved/Requests/
   Messages/Account tabs + أي فعل جوه Home بيؤدي لحاجة شخصية) — لو حاول يدخلها
   من غير حساب، وديه لـ `/sign-in?next=<المسار اللي كان رايحله>` (زي منطق
   `next` الموجود في `app/src/pages/SignIn.tsx` بالظبط).
3. بعد ما يسجّل دخول من `/sign-in`، رجّعه لنفس `next` مش دايمًا `/requests`
   (`mobile/client/app/sign-in.tsx` دلوقتي بيعمل `router.replace("/requests")`
   ثابتة — لازم تقرا الـ `next` بنفس فكرة الموقع).
4. `usePushNotifications()` جوه `(tabs)/_layout.tsx` بتفترض إن فيه `customer` —
   تأكد إنها لسه بتشتغل صح (no-op) لما يكون فيه زوّار من غير حساب.

## معايير القبول
- [ ] فتح التطبيق من غير حساب يوديك على "الرئيسية" مباشرة، مش على تسجيل الدخول.
- [ ] تقدر تتصفح الخدمات، الشركات، بروفايل شركة، عن العاصمة، تواصل معنا — من
      غير أي redirect.
- [ ] الضغط على "احفظ"/"ابعت طلب"/تاب "طلباتي"/"الرسائل"/"حسابي" من غير حساب
      يوديك لشاشة sign-in، وبعد ما تسجّل بيرجّعك بالظبط لنفس المكان.
- [ ] `npx tsc --noEmit` نضيف.

## رسالة الـ ship
"mobile: allow guest browsing, gate only account-specific actions (parity with website)"
```

---

## 🖼️ PHASE 2 — اللوجو الفعلي بدل النص

```
## الهدف
التطبيق دلوقتي مفيهوش أي مكان بيعرض صورة اللوجو الحقيقي (اللي الأدمن رافعه من
لوحة التحكم). صفحة تسجيل الدخول (`mobile/client/app/sign-in.tsx`) بتعرض كلمة
"العاصمة" كنص (`styles.brand`) بس. الموقع بيستخدم `app/src/components/Logo.tsx`
اللي بيقرا `logo_url` و`site_name` و`logo_scale` من `useSettings()`
(`app/src/lib/settings.ts`) وبيعرض `<img src={logo_url || "/logo.png"} />`.

نقطة مهمة: الـ endpoint موجود بالفعل جوه الموبايل — `mobile/client/lib/pages.ts`
فيه `fetchPlatformSettings()` بيضرب `/settings` (نفس اللي الويب بيستخدمه)،
والـ type `ApiPlatformSettings` من `@alassema/core` المفروض فيه `logo_url` و
`logo_scale` جاهزين. المشكلة إنه مش مستخدم غير في `contact.tsx`.

## المهام
1. اعمل `mobile/client/lib/settings.ts`: hook بسيط `useSettings()` بيعمل fetch
   لـ `fetchPlatformSettings()` مرة واحدة (مع cache بسيط في الذاكرة، زي باقي
   الـ libs هناك)، ويرجّع `{ logo_url, site_name, logo_scale }` مع defaults
   معقولة لو لسه بيحمّل.
2. اعمل `mobile/client/components/Logo.tsx`: بيستخدم `useSettings()`، ويعرض
   `<Image source={{ uri: logo_url }} />` لو موجود، وإلا fallback لأيقونة/نص
   العلامة الحالي. طبّق `logo_scale` كـ transform زي الويب بالظبط لو الوقت
   يسمح، وإلا سيبها ملاحظة.
3. بدّل النص `styles.brand` في `sign-in.tsx` باستخدام `<Logo>` فوق العنوان،
   بنفس الترتيب اللي في `app/src/pages/SignIn.tsx` (اللوجو فوق، بعدين العنوان،
   بعدين الوصف).
4. ضيف `<Logo>` صغيرة في هيدر كل تاب رئيسي (`(tabs)/home.tsx` وأخواتها) —
   في مكان مناسب أعلى الشاشة، زي وجود اللوجو الدائم في TopNav بتاع الويب.
   ده مش لازم يبقى nav bar كامل، بس اللوجو لازم يبقى ظاهر وإنت في التطبيق،
   مش بس في صفحة تسجيل الدخول.

## معايير القبول
- [ ] صفحة تسجيل الدخول بتعرض صورة اللوجو الحقيقي (نفس اللي في الموقع)، مش نص.
- [ ] لو الأدمن يغيّر اللوجو من لوحة التحكم، التطبيق يعكس التغيير من غير build
      جديد (لأنه بيتقرا من الـ API وقت التشغيل).
- [ ] اللوجو ظاهر في الشاشات الرئيسية للتطبيق مش بس شاشة الدخول.
- [ ] `npx tsc --noEmit` نضيف.

## رسالة الـ ship
"mobile: show real branding logo from settings instead of hardcoded text"
```

---

## 📱 PHASE 3 — ترتيب وتشكيلة أزرار التاب بار (checkpoint بسيط)

```
## الهدف
التاب بار السفلي مختلف تمامًا بين الاتنين:
- الموقع (نسخة الموبايل من المتصفح، `app/src/RootLayout.tsx` → `PublicBottomNav`):
  الرئيسية، الخدمات، المفضلة، طلباتي (4 بس — الرسائل والحساب موجودين في
  الـ TopNav/الحساب مش تاب سفلي).
- التطبيق (`mobile/client/app/(tabs)/_layout.tsx`): الرئيسية، الشركات، طلباتي،
  الرسائل، المفضلة، حسابي (6).

## قبل التنفيذ
اسأل مازن اختيار واحد من التلاتة (وقدّم رأيك: الخيار ب هو الأقرب لمنطق التطبيق
الحالي اللي بيعتمد على الشركات كنقطة دخول لبعت الطلب):
- (أ) نفس الـ 4 بتوع الموقع بالظبط (الرئيسية، الخدمات، المفضلة، طلباتي)،
  والرسائل والحساب يتنقلوا لقايمة/زرار حساب زي الموقع.
- (ب) نسيب الـ 6 الحاليين بس نرتّبهم بنفس منطق أولوية الموقع (الرئيسية أول
  حاجة، بعدين الخدمات/الشركات، وبعدين المفضلة/طلباتي، والرسائل والحساب
  آخر اتنين).
- (ج) حاجة تالتة يقترحها مازن.

## المهام (بعد ما ياخد القرار)
عدّل `mobile/client/app/(tabs)/_layout.tsx` بالترتيب والتشكيلة المتفق عليها.
لو اتشال تاب "الشركات"، تأكد إن فيه طريقة واضحة تانية توصّل له (مثلًا من
الرئيسية أو من تاب الخدمات) — مش تختفي كصفحة.

## معايير القبول
- [ ] الترتيب مطابق للقرار المتفق عليه مع مازن.
- [ ] كل الشاشات الحالية (الشركات، الخدمات، الرسائل، الحساب) لسه ممكن توصلها
      بسهولة حتى لو مش تاب سفلي مباشر.
- [ ] `npx tsc --noEmit` نضيف.

## رسالة الـ ship
"mobile: align bottom tab bar order/composition with website navigation"
```

---

## 🏢 PHASE 4 — فلاتر وترتيب و pagination في صفحة الشركات

```
## الهدف
`app/src/pages/Companies.tsx` فيه: فلتر فئة الخدمة، فلتر أقل تقييم، فلتر "متاح
دلوقتي"، ترتيب (موصى به/تقييم/مشاريع/مراجعات/اسم)، وترقيم صفحات — كل ده فوق
البحث. `mobile/client/app/(tabs)/companies.tsx` فيه مربع بحث بس (debounced،
350ms) وقايمة واحدة طويلة.

## المهام
1. ضيف صف فلاتر أفقي قابل للسحب فوق قايمة الشركات (زي الـ chips المستخدمة في
   `guided-start.tsx` كمرجع أسلوب) بفلاتر: الفئة (من `fetchCategories()`)،
   أقل تقييم، "متاح دلوقتي".
2. ضيف زرار/شيت ترتيب (استخدم `Modal` زي الموجود في `new-request/[slug].tsx`
   لاختيار الحي، كمرجع نمط الـ bottom sheet المستخدم في المشروع) بنفس خيارات
   الويب: موصى به، الأعلى تقييمًا، الأكتر مشاريع، الأكتر مراجعات، الاسم.
3. `fetchCompanies()` في `mobile/client/lib/companies.ts` — تأكد إنها بتقبل
   نفس الباراميترات اللي الـ `/companies` endpoint بياخدها في الويب (category,
   minRating, sort) ووسّعها لو ناقصة، بدل ما تعمل فلترة/ترتيب client-side.
4. ضيف "حمّل المزيد" أو ترقيم صفحات بسيط (infinite scroll على FlatList بـ
   `onEndReached` أسهل تجربة على موبايل من أزرار الصفحات).

## معايير القبول
- [ ] تقدر تفلتر بالفئة والتقييم و"متاح دلوقتي"، وتغيّر الترتيب، والنتايج
      بتتحدّث فعليًا من السيرفر.
- [ ] قايمة طويلة (أكتر من صفحة واحدة) بتحمّل المزيد من غير ما توقف عند أول
      دفعة.
- [ ] `npx tsc --noEmit` نضيف.

## رسالة الـ ship
"mobile: add category/rating filters, sort, and pagination to Companies (parity with website)"
```

---

## 🏬 PHASE 5 — بروفايل الشركة: تنقل بين الأقسام + سلة طلبات

```
## الهدف
`app/src/pages/CompanyProfile.tsx` فيه section nav (`SectionNav.tsx`) للقفز بين
الأقسام، وسلة (`useCart` من `app/src/lib/cart.ts`) بتسمح تختار أكتر من خدمة
وشريط ثابت (`RequestBar.tsx`) بيجمعهم قبل الإرسال. `mobile/client/app/company/
[slug].tsx` بيعرض نفس الأقسام (عن الشركة، الخدمات والأسعار، معرض الأعمال، آراء
العملاء) بس سكرول عادي من غير تنقل سريع، وزرار "اطلب الخدمة" واحد بيودّي على
`new-request/[slug].tsx` من غير اختيار أكتر من خدمة سوا.

## المهام
1. ضيف شريط تابات صغير أعلى الصفحة (عن الشركة / الأسعار / المعرض / التقييمات)
   بيعمل scroll-to لنفس الـ section — استخدم `scrollTo`/`measureLayout` على
   الـ `ScrollView` الموجودة، مفيش داعي لمكتبة جديدة.
2. لو الوقت يسمح: اربط `OfferingPicker` (`mobile/client/components/
   OfferingPicker.tsx`) — موجود بالفعل ومستخدم جوه `new-request/[slug].tsx` —
   داخل صفحة البروفايل نفسها بدل ما يكون بس جوه فورم الطلب، عشان العميل يقدر
   يختار أكتر من خدمة وهو لسه بيقرا عن الشركة، مطابقةً لتجربة الويب. لو ده هيبقى
   تغيير كبير في وقت الفيز، وثّقه في الملاحظات وسيبه لفيز منفصلة بدل ما تلخبط
   `new-request` الحالية.

## معايير القبول
- [ ] الضغط على أي تاب في الشريط بيودّيك فورًا لنفس القسم في الصفحة.
- [ ] باقي محتوى الصفحة (الإحصائيات، البانر لو الشركة مشغولة، زرار الطلب)
      شغالين زي ما هما.
- [ ] `npx tsc --noEmit` نضيف.

## رسالة الـ ship
"mobile: add in-page section navigation to company profile (parity with website)"
```

---

## 🔎 PHASE 6 — بحث شامل (Global Search)

```
## الهدف
الموقع فيه `SearchOverlay.tsx` بيدور في كل حاجة (شركات + خدمات) من أي صفحة،
باختصار كيبورد "/" كمان. التطبيق مفيهوش بحث غير جوه تاب "الشركات" بس، وبيدور
في الشركات بس مش الخدمات.

## المهام
1. اعمل شاشة/Modal بحث جديدة (`mobile/client/app/search.tsx` أو Modal من فوق
   أي تاب) بتدور في الشركات والخدمات مع بعض (استخدم `fetchCompanies()` و
   `fetchCategories()` الموجودين، أو اعمل endpoint موحّد لو الباك إند فيه
   `/search` زي ما فيه `app/src/lib/search.ts` على الويب — افحص الملف ده الأول
   قبل ما تكرر منطق).
2. ضيف زرار بحث واضح في هيدر التابات الرئيسية (يفتح الشاشة/الـ Modal دي).

## معايير القبول
- [ ] تقدر توصل للبحث من أي تاب رئيسي بضغطة واحدة.
- [ ] البحث بيرجّع نتايج شركات وخدمات مع بعض، وكل نتيجة بتودّيك للصفحة الصح.
- [ ] `npx tsc --noEmit` نضيف.

## رسالة الـ ship
"mobile: add unified search across companies and services (parity with website)"
```

---

## ✉️ PHASE 7 — صفحة تأكيد الإيميل جوه التطبيق

```
## الهدف
لينك تأكيد الإيميل اللي بيتبعت للعميل بيوديه لـ `/verify-email?token=...` على
الويب (`app/src/pages/VerifyEmail.tsx`)، اللي بيستدعي `verifyEmailToken(token)`
وبعدين يسجّله دخول ويوديه لـ `/requests`. التطبيق مفيهوش شاشة زي دي خالص — لو
حد فاتح التطبيق ضغط على اللينك، بيتفتح في المتصفح مش جوه التطبيق.

## المهام
1. اتأكد من إعدادات الـ deep link/universal link بتاعة التطبيق (`app.json` —
   الـ `scheme` أو `associated domains`) هل مربوطة بدومين الإيميل ولا لأ.
2. اعمل `mobile/client/app/verify-email.tsx` بيقرا `token` من
   `useLocalSearchParams` أو من الـ deep link params، ويستدعي نفس
   `verifyEmailToken` الموجودة في `mobile/client/lib/customerAuth.ts` (لو
   مش موجودة هناك، ضيفها بنفس شكل نسختها في `app/src/lib/customerAuth.ts`)،
   وبعد النجاح `router.replace("/requests")`.
3. لو الـ backend بيبني لينك الإيميل بصيغة ويب ثابتة (`sendCustomerVerification
   Email` في الـ API — الملف مذكور في كومنت `app/src/router.tsx`)، اتأكد مع
   مازن هل نحتاج نضيف صفحة وسيطة على الويب بتفتح التطبيق (Universal Link) لو
   العميل فاتح اللينك من موبايله وعنده التطبيق متسطّب.

## معايير القبول
- [ ] فتح لينك تأكيد الإيميل الصحيح جوه التطبيق (مش المتصفح) بيسجّل الدخول
      ويوديك لـ "طلباتي".
- [ ] لينك منتهي/غلط بيعرض رسالة واضحة وزرار يرجّعك لتسجيل الدخول، زي سلوك
      الويب بالظبط.
- [ ] `npx tsc --noEmit` نضيف.

## رسالة الـ ship
"mobile: handle email verification links inside the app (parity with website)"
```

---

## ✅ PHASE 8 — مراجعة نهائية جنب بعض

```
## الهدف
مراجعة شاملة: افتح كل صفحة في التطبيق جنب الصفحة المقابلة في الويب (على عرض
موبايل ~390px) وقارن.

## المهام
1. اعمل جدول (ملف `MOBILE-WEB-PARITY-QA.md` جديد) بعمودين: صفحة الموبايل ↔
   صفحة الويب المقابلة، ولكل صف حط ✅/❌ + ملاحظة قصيرة.
2. أي فرق باقي (حتى لو صغير — لون، مسافة، ترتيب نص) سجّله في نفس الملف تحت
   "ملاحظات متبقية" بدل ما تصلحه بنفسك من غير ما تسأل — بعض الفروق ممكن تكون
   مقصودة لظروف الموبايل (زي عدم وجود hover states).
3. اعرض الملف على مازن.

## معايير القبول
- [ ] كل صفحة من الـ 7 فيزات فوق اتراجعت فعليًا على محاكي (مش قراءة كود بس).
- [ ] الملف موجود وواضح.

## رسالة الـ ship
"mobile: final QA pass against website parity checklist"
```
