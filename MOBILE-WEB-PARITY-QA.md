# مراجعة تطابق الموبايل مع الويب — Phase 8

> ⚠️ **تنبيه مهم عن طريقة المراجعة دي:** الفيز طلبت مراجعة فعلية على محاكي
> (مش قراءة كود بس). البيئة اللي شغّال فيها الـ agent مفيهاش أداة تشغيل
> محاكي/متصفح تفاعلية، فمازن وافق (2026-08-18) على إني أعمل الجدول ده من
> **قراءة ومقارنة الكود** بين `mobile/client/app/**` و `app/src/pages/**`
> بدل التشغيل الفعلي. يعني:
> - عمود ✅/❌ هنا معناه "الكود بيعمل نفس السلوك/المحتوى منطقيًا" مش "شافيته
>   شغّال على الشاشة".
> - أي حاجة بصرية بحتة (ألوان دقيقة، أنيميشن، مسافات فعلية على شاشة 390px)
>   **محتاجة مراجعة حقيقية على محاكي أو جهاز** — دي مش مغطاة هنا. شايف
>   قائمة "لسه محتاج تجربة فعلية" في الآخر.
> - لو حد شغّل المحاكي فعليًا، الأفضل إنه يحدّث الجدول ده بنتائج حقيقية
>   بدل الاعتماد على القراءة بس.

---

## الجدول

| # | صفحة الموبايل | صفحة الويب المقابلة | ✅/❌ | ملاحظة |
|---|---|---|---|---|
| 1 | `app/index.tsx` (redirect لـ `/home`) | `RootLayout.tsx` (تصفح ضيف) | ✅ | Phase 1 — تصفح بدون حساب متطابق منطقيًا. |
| 2 | `(tabs)/home.tsx` | `pages/Home.tsx` | ⚠️ | بورت مبسّط عمدًا (مذكور في كومنت الملف نفسه) — فروق حقيقية أكتر تحت. |
| 3 | `(tabs)/companies.tsx` | `pages/Companies.tsx` | ⚠️ | الفلاتر/الترتيب/الـ pagination (Phase 4) متطابقة منطقيًا، بس شكل الكارت مختلف جدًا. |
| 4 | `app/company/[slug].tsx` + `app/company/[slug]/waitlist.tsx` | `pages/CompanyProfile.tsx` | ⚠️ | Section nav (Phase 5) موجود؛ شاشة الـ waitlist موجودة وشغّالة أصلًا (تصحيح — شوف الملاحظة). فيه محتوى ناقص تاني (تحت). |
| 5 | `app/new-request/[slug].tsx` | `pages/RequestForm.tsx` | ⚠️ | الحقول الأساسية موجودة؛ ناقص captcha وحقول تانية. |
| 6 | `(tabs)/requests.tsx` | `pages/MyRequests.tsx` | ⚠️ | الفلترة بالحالة موجودة؛ ناقص عرض السعر التقديري والبحث. |
| 7 | `(tabs)/messages.tsx` + `chat/[leadId].tsx` | `pages/Messages.tsx` | ✅ | بنية مختلفة (push لشاشة تانية بدل list/thread panes) بس نفس القدرة. |
| 8 | `(tabs)/saved.tsx` | `pages/Saved.tsx` | ⚠️ | ناقص بحث، تقييم، وزرار طلب سريع على الكارت. |
| 9 | `(tabs)/account.tsx` | `pages/Account.tsx` | ⚠️ | قريب جدًا؛ ناقص صورة جوجل الحقيقية (avatarUrl). |
| 10 | `app/sign-in.tsx` | `pages/SignIn.tsx` | ✅ | مطابقة قوية (اللوجو Phase 2، next-redirect Phase 1). |
| 11 | `app/about.tsx` | `pages/About.tsx` | ⚠️ | نفس الفكرة؛ النصوص مش نفس نصوص i18n بالظبط. |
| 12 | `app/contact.tsx` | `pages/Contact.tsx` | ⚠️ | ناقص روابط السوشيال ميديا بالكامل. |
| 13 | `app/services/index.tsx` | `pages/Services.tsx` | ✅ | مطابقة قوية. |
| 14 | `app/services/[slug].tsx` | `pages/ServiceCategory.tsx` | ⚠️ | ناقص badge التوثيق، chips الخدمات، وpagination. |
| 15 | `app/guided-start.tsx` | `pages/GuidedStart.tsx` | ✅ | نفس البنية (فئة → أولوية → 3 ترشيحات) — مش متراجع سطر بسطر بالكامل. |
| 16 | `app/search.tsx` | `components/SearchOverlay.tsx` | ✅ | Phase 6 — مطابقة وظيفية؛ ناقص "الأكتر بحثًا" (مش موجود أصلًا بمحتوى عربي على الويب). |
| 17 | `app/verify-email.tsx` | `pages/VerifyEmail.tsx` | ✅ | Phase 7 — نفس النصوص والسلوك؛ الـ Universal Link لسه مش موصول (موثّق ومتفق عليه). |
| 18 | `app/legal/[kind].tsx` | `pages/LegalPage.tsx` | ❌ | الموبايل بيعرض النص خام (Text عادي)؛ الويب بيرندر Markdown فعلي. |
| 19 | `(tabs)/_layout.tsx` (تاب بار) | `PublicBottomNav` / `TopNav` | ✅ | Phase 3 — قرار مقصود (6 تابات بدل 4)، موثّق ومتفق عليه. |

---

## ملاحظات متبقية (فروق حقيقية لقيتها في الكود)

### فروق محتوى ملموسة (مش مجرد شكل)

1. ~~**`company/[slug].tsx`** — زرار "الانضمام لقائمة الانتظار" بيودّي على
   مسار مش موجود~~ — **تراجعت عن الكلام ده.** ده كان **خطأ مني** مبني على
   بحث ناقص في الجلسة اللي فاتت (استخدمت `find -maxdepth 2` وقتها، واللي
   ملهاش وصول لملف جوّه مجلد `[slug]/` تلات مستويات تحت `app/`).
   `mobile/client/app/company/[slug]/waitlist.tsx` **كان موجود بالفعل**
   من commit `df02b9d` (فورم كامل: اسم، موبايل، ملاحظة) وبيستخدم
   `joinWaitlist()` الموجودة أصلًا في `lib/companyDetail.ts` — المسار مكانش
   مكسور خالص. كنت بدأت أعمل نسخة تانية بديلة (فورم فيه اختيار خدمة كمان)
   وضفت `joinWaitlist` مكرّرة في `lib/waitlist.ts`، لكن رجّعت الملفين
   الاتنين لحالتهم الأصلية بـ `git checkout` قبل ما أرفع — عشان منسيبش
   كود مكرّر لنفس الوظيفة. لو حد عايز يضيف اختيار الخدمة للفورم الموجود
   فعلًا، ده تحسين بسيط منفصل، مش إصلاح مسار مكسور.
2. **`company/[slug].tsx`** — قسم "مشاريع منجزة" (`company.projects`) مش
   متعروض خالص؛ الموبايل بيعرض `company.gallery` بس. الويب بيعرض الاتنين
   منفصلين.
3. **`company/[slug].tsx`** — مفيش قسم "بيانات التواصل" (تليفون/عنوان)
   زي اللي في نص صفحة الويب.
4. **`new-request/[slug].tsx`** (وكمان `SiteReviewModal`/`FeedbackModal`
   وشاشة الـ waitlist الجديدة) — مفيش Captcha. اتأكدت من
   `api/src/lib/middleware/captcha.ts`: الـ verification بتاعته no-op
   تمامًا لو `TURNSTILE_SECRET_KEY`/`RECAPTCHA_SECRET_KEY` مش متظبطين —
   وهما مش موجودين في `api/.env.production.reference` خالص، يعني
   **حاليًا مفيش مشكلة فعلية**. لو حد ظبط أي مفتاح captcha على السيرفر
   في المستقبل، كل الطلبات دي (lead، مراجعة، waitlist) من الموبايل هترفض
   فورًا بـ 400 من غير تحذير — نقطة لازم تتفتكر وقتها.
5. **`new-request/[slug].tsx`** — قايمة الأحياء (`DISTRICTS`) ثابتة من
   `@alassema/core`؛ الويب بيقرا `settings.districts` (قابلة للتعديل من
   لوحة التحكم) مع fallback للقايمة الثابتة. لو أدمن عدّل الأحياء من
   اللوحة، الموبايل مش هيعكس التعديل.
6. **`(tabs)/requests.tsx`** — مفيش عرض للسعر التقديري (`estimatedMin/Max`،
   خصم الباقة، "بنود بعد المعاينة") على كارت الطلب، رغم إن الفيز اللي
   قبل الـ7 فيزات دي ("priced-catalog request flow") بنت الـ backend/الـ
   flow ده بالفعل. العميل بيبعت طلب بعناصر مسعّرة بس مايشوفهاش تاني.
7. **`(tabs)/requests.tsx`** — مفيش بحث (search box) في الطلبات، بعكس
   الويب.
8. **`(tabs)/saved.tsx`** — الصف بيعرض الاسم والفئة بس — **مفيش تقييم
   (نجوم/رقم) خالص**، ومفيش زرار "احفظ/الغِ الحفظ" مباشر من الصف (لازم
   تدخل بروفايل الشركة)، ومفيش زرار "اطلب الخدمة" مباشر زي الويب.
9. **`app/contact.tsx`** — روابط السوشيال ميديا (فيسبوك/انستجرام/تويتر/
   لينكدإن) مش متعروضة خالص، رغم إن `ApiPlatformSettings` فيها الحقول دي
   وبتتقرا في `useSettings()`.
10. **`app/services/[slug].tsx`** — مفيش pagination؛ بيحمّل أول 50 شركة
    بس (`pageSize: 50`) من غير طريقة تشوف الباقي لو القسم فيه أكتر من كده.
    الويب عنده `Pagination` كاملة.
11. **`app/legal/[kind].tsx`** — المحتوى بيتعرض كـ `Text` خام. لو أدمن كتب
    الشروط/الخصوصية بصيغة Markdown (زي ما الويب بيتوقع عبر مكوّن
    `Markdown`)، الرموز (`**`, `-`, `#`) هتظهر كنص حرفي في الموبايل بدل
    ما تترندر.
12. **`(tabs)/account.tsx`** — بيعرض دايمًا دايرة بحرف الاسم، مش صورة
    جوجل الحقيقية (`customer.avatarUrl`) حتى لو موجودة.

### فروق شكل/تفصيل (أولوية أقل، غالبًا مقصودة لظروف الموبايل)

13. **`(tabs)/home.tsx`** — مفيش الإحصائيات المتحركة (شركاء/مشاريع/تقييم/
    فئات)، مفيش الـ hero الكامل بصورة الأفق وسكرول-cue، مفيش marquee
    مستمرة للتقييمات (الموبايل عارضها Row عادي)، مفيش قسم "ليه العاصمة"
    داخل الصفحة نفسها (موجود بس في `about.tsx` كصفحة منفصلة).
14. **`(tabs)/companies.tsx`** — كارت الشركة في الموبايل صف مضغوط (لوجو
    44px + اسم + فئة). كارت الويب (حتى في عرض موبايل) كارت طويل بصورة
    غلاف، لوجو فوقها، زرار حفظ، badge "مشغول"/"موثّق"، تقييم، وصف قصير،
    وعدد مشاريع. فرق بصري كبير، مش بس تفصيل.
15. **`(tabs)/account.tsx`** — فيه زرار "تسجيل الخروج" مباشر وواضح، وهو
    زيادة مفيدة عن الويب (اللي معندوش زرار خروج مستقل واضح غير من جوه
    "تسجيل الخروج من كل الأجهزة"). ده تحسين، مش نقص.
16. **`app/about.tsx`** — النصوص (العناوين والوصف لـ4 الأسباب) مكتوبة
    يدويًا في الموبايل، مش نفس نصوص `home_why_1..4` بالظبط من `i18n.ts`.
    كمان `(tabs)/home.tsx` عنده array تاني بـ3 أسباب بنصوص مختلفة شوية
    عن `about.tsx` (4 أسباب) — يعني الموبايل نفسه مش متسق داخليًا في
    نص "ليه العاصمة".
17. **`app/services/[slug].tsx`** — مفيش badge "موثّق" ولا chips الخدمات
    الفردية (زي "دهانات"، "تكييفات") على كارت الشركة، موجودين في الويب.

---

## لسه محتاج تجربة فعلية على محاكي (مش هيبان من الكود)

- إحساس اللمس/التمرير الفعلي (السرعة، الـ momentum، حجم لمس الأزرار).
- تلوين فعلي في الوضعين (مفيش dark mode في التطبيق أصلًا — تأكيد إن ده مقصود).
- سلوك لوحة المفاتيح مع كل فورم (خصوصًا `new-request` والـ Modal بتاعت الترتيب في `companies.tsx`).
- أداء الـ infinite scroll في `companies.tsx` مع قايمة طويلة فعلية.
- تجربة الإشعارات (push) الفعلية عند وصول رسالة.
- الـ deep link الحقيقي بعد ما يتربط الدومين (Phase 7).

---

**الحالة:** الجدول ده جاهز يتعرض على مازن. بند #1 (مسار الـ waitlist)
طلع false alarm وتصحّح — مفيش تعديل كود فعلي عليه في الآخر. بند #4
(الـ captcha) اتأكد إنه مش مشكلة فعلية دلوقتي. الباقي قرار مفتوح: أي بند
من "فروق محتوى ملموسة" يستاهل فيز تصحيح منفصلة، وأنهي بند نسيبه زي ما هو.
