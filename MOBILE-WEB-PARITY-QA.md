# مراجعة تطابق الموبايل مع الويب — Phase 8 + Phase 9

> ⚠️ **تنبيه مهم عن طريقة المراجعة دي:** الفيز 8 طلبت مراجعة فعلية على محاكي
> (مش قراءة كود بس). البيئة اللي شغّال فيها الـ agent مفيهاش أداة تشغيل
> محاكي/متصفح تفاعلية، فمازن وافق (2026-08-18) على إني أعمل الجدول ده من
> **قراءة ومقارنة الكود** بدل التشغيل الفعلي. نفس القيد لسه ساري في فيز 9 —
> شايف قائمة "لسه محتاج تجربة فعلية" في الآخر، وهي **لسه فاضية من نتايج
> حقيقية** لحد ما حد يشغّل التطبيق فعليًا على محاكي/جهاز.

---

## 🔴 فيز 9 — أخطر حاجة اتصلحت: شاشة تأكيد السعر النهائي

كان مفيش أي تعامل مع `completion.verificationStatus` في التطبيق خالص —
عميل عنده طلب مكتمل بسعر نهائي كان يقدر يستخدم التطبيق عادي من غير أي حاجز.
اتصلح دلوقتي:

- `mobile/client/components/PriceVerificationGate.tsx` (جديد) — نفس الأربع
  مراحل بتاعة الويب (تأكيد المبلغ / بلاغ خلاف / تأكيد النتيجة / تقييم اختياري)،
  بيستخدم `ReviewModal` الموجود أصلًا للمرحلة الأخيرة بدل ما يتبني من الصفر.
  مفيش زرار رجوع/إغلاق في أول مرحلتين، زي الويب بالظبط.
- `mobile/client/app/_layout.tsx` — بعد ما الـ session يجهز، بيدوّر على أول
  lead بحالة `PENDING` ويعرض الشاشة دي بدل الـ `<Stack>` كله، وبيفضل يسمع
  live events عشان يمسك أي completion يحصل والتطبيق شغّال.
- **اكتشاف مهم غيّر الحل المقترح في نص الفيز:** `/leads/verify` بيرفض أي
  محاولة بـ `phone` لوحده لو الـ lead عنده `trackingToken` مسجّل — وكل
  lead بيتسجّل بتوكن دلوقتي (`generateTrackingToken()` بتشتغل دايمًا وقت
  الإنشاء)، يعني اقتراح "استخدم phone كبديل" كان هيفشل بـ 404 لأي طلب حقيقي.
  الحل الفعلي: `lib/leadTokens.ts` (جديد) بيحفظ الـ token محليًا (AsyncStorage)
  لحظة إنشاء الطلب — بالظبط زي إزاي الويب بيحفظه في localStorage. `phone` لسه
  بيتبعت كـ fallback لو مفيش token محفوظ، بس ده هيشتغل بس لو الـ lead من
  النوع القديم (نادر جدًا).
- **قيد معروف، مش عيب جديد:** لو عميل مسح التطبيق وسطّبه تاني، أو بعت الطلب
  من جهاز وبيفتح من جهاز تاني، مفيش token محفوظ محليًا على الجهاز الجديد —
  ونفس القيد ده موجود بالظبط على الويب (localStorage بتاعته بينمسح لو الكاش
  اتمسح). مش حاجة الموبايل لازم يحلها لوحده.

---

## الجدول

| # | صفحة الموبايل | صفحة الويب المقابلة | ✅/❌ | ملاحظة |
|---|---|---|---|---|
| 0 | `components/PriceVerificationGate.tsx` + `app/_layout.tsx` | `RootLayout.tsx` + `components/priceVerification/*` | ✅ | Phase 9 — جديد بالكامل. القيد الوحيد: token محلي (شوف فوق). |
| 1 | `app/index.tsx` (redirect لـ `/home`) | `RootLayout.tsx` (تصفح ضيف) | ✅ | Phase 1 — تصفح بدون حساب متطابق منطقيًا. |
| 2 | `(tabs)/home.tsx` | `pages/Home.tsx` | ⚠️ | بورت مبسّط عمدًا (مذكور في كومنت الملف نفسه) — فروق حقيقية أكتر تحت. |
| 3 | `(tabs)/companies.tsx` | `pages/Companies.tsx` | ⚠️ | الفلاتر/الترتيب/الـ pagination (Phase 4) متطابقة منطقيًا، بس شكل الكارت مختلف جدًا. |
| 4 | `app/company/[slug].tsx` + `app/company/[slug]/waitlist.tsx` | `pages/CompanyProfile.tsx` | ⚠️ | Section nav (Phase 5) موجود؛ قسم المشاريع اتضاف (Phase 9)؛ لسه ناقص قسم بيانات التواصل. |
| 5 | `app/new-request/[slug].tsx` | `pages/RequestForm.tsx` | ⚠️ | الإجمالي التقديري بيتعرض بعد الإرسال (Phase 9)؛ لسه ناقص captcha وربط الأحياء بلوحة التحكم. |
| 6 | `(tabs)/requests.tsx` | `pages/MyRequests.tsx` | ⚠️ | الإجمالي التقديري بيتعرض على الكارت (Phase 9)؛ لسه ناقص بحث. |
| 7 | `(tabs)/messages.tsx` + `chat/[leadId].tsx` | `pages/Messages.tsx` | ✅ | بنية مختلفة (push لشاشة تانية بدل list/thread panes) بس نفس القدرة. |
| 8 | `(tabs)/saved.tsx` | `pages/Saved.tsx` | ⚠️ | تقييم + زرار "شيل من المفضلة" سريع اتضافوا (Phase 9)؛ لسه ناقص بحث وزرار "اطلب الخدمة" على الكارت. |
| 9 | `(tabs)/account.tsx` | `pages/Account.tsx` | ⚠️ | قريب جدًا؛ ناقص صورة جوجل الحقيقية (avatarUrl). |
| 10 | `app/sign-in.tsx` | `pages/SignIn.tsx` | ✅ | مطابقة قوية (اللوجو Phase 2، next-redirect Phase 1). |
| 11 | `app/about.tsx` | `pages/About.tsx` | ⚠️ | نفس الفكرة؛ النصوص مش نفس نصوص i18n بالظبط. |
| 12 | `app/contact.tsx` | `pages/Contact.tsx` | ✅ | روابط السوشيال ميديا اتضافت (Phase 9). |
| 13 | `app/services/index.tsx` | `pages/Services.tsx` | ✅ | مطابقة قوية. |
| 14 | `app/services/[slug].tsx` | `pages/ServiceCategory.tsx` | ⚠️ | Pagination اتضافت (Phase 9)؛ لسه ناقص badge التوثيق وchips الخدمات. |
| 15 | `app/guided-start.tsx` | `pages/GuidedStart.tsx` | ✅ | نفس البنية (فئة → أولوية → 3 ترشيحات) — مش متراجع سطر بسطر بالكامل. |
| 16 | `app/search.tsx` | `components/SearchOverlay.tsx` | ✅ | Phase 6 — مطابقة وظيفية؛ ناقص "الأكتر بحثًا" (مش موجود أصلًا بمحتوى عربي على الويب). |
| 17 | `app/verify-email.tsx` | `pages/VerifyEmail.tsx` | ✅ | Phase 7 — نفس النصوص والسلوك؛ الـ Universal Link لسه مش موصول (موثّق ومتفق عليه). |
| 18 | `app/legal/[kind].tsx` | `pages/LegalPage.tsx` | ✅ | Phase 9 — بيرندر Markdown فعلي دلوقتي (`react-native-markdown-display`). |
| 19 | `(tabs)/_layout.tsx` (تاب بار) | `PublicBottomNav` / `TopNav` | ✅ | Phase 3 — قرار مقصود (6 تابات بدل 4)، موثّق ومتفق عليه. |

---

## ملاحظات متبقية (لسه مفتوحة بعد فيز 9)

هدف فيز 9 كان "قسم ملاحظات متبقية فاضي أو فيه بس حاجات جديدة بسبب واضح".
دي مش فاضية تمامًا — البنود دي **مقصود إنها اتسابت** لإنها مش من ضمن مهام
فيز 9 المكتوبة صراحةً (فقط الـ 6 بنود المذكورة في نص الفيز + شاشة تأكيد
السعر اتصلحوا). سجلتها هنا عشان تتقيّم في فيز لاحقة:

1. **`company/[slug].tsx`** — لسه مفيش قسم "بيانات التواصل" (تليفون/عنوان)
   في نص صفحة البروفايل، زي اللي في الويب.
2. **`new-request/[slug].tsx`** (وكمان `SiteReviewModal`/`FeedbackModal`
   وشاشة الـ waitlist) — مفيش Captcha حقيقي. اتأكد إن ده مش مشكلة فعلية
   **دلوقتي** (`TURNSTILE_SECRET_KEY`/`RECAPTCHA_SECRET_KEY` مش متظبطين في
   `api/.env.production.reference`) — بس **فيز 10 (موجودة في
   `MOBILE-WEB-PARITY-PROMPT.md`) بتغطي تنفيذ الكابتشا الحقيقي**، متتنفّذش
   هنا لإنها خارج نطاق فيز 9.
3. **`new-request/[slug].tsx`** — قايمة الأحياء (`DISTRICTS`) ثابتة من
   `@alassema/core`؛ الويب بيقرا `settings.districts` (قابلة للتعديل من
   لوحة التحكم).
4. **`(tabs)/requests.tsx`** — مفيش بحث (search box) في الطلبات.
5. **`(tabs)/saved.tsx`** — زرار "اطلب الخدمة" المباشر على الكارت (زي
   الويب) لسه مش موجود — بس التقييم وزرار الحفظ السريع اتضافوا.
6. **`(tabs)/account.tsx`** — بيعرض دايمًا دايرة بحرف الاسم، مش صورة جوجل
   الحقيقية (`customer.avatarUrl`) حتى لو موجودة.
7. **`(tabs)/home.tsx`** — مفيش الإحصائيات المتحركة، الـ hero الكامل،
   marquee التقييمات، أو قسم "ليه العاصمة" جوه الصفحة نفسها (موجود بس في
   `about.tsx` منفصل).
8. **`(tabs)/companies.tsx`** — كارت الشركة صف مضغوط، مش الكارت الطويل
   بصورة غلاف/badges اللي في الويب.
9. **`app/about.tsx`** — نصوص الأسباب الأربعة مكتوبة يدويًا، مش نفس نصوص
   `home_why_1..4` من `i18n.ts` بالظبط، وغير متسقة مع الـ 3 أسباب المختلفة
   في `(tabs)/home.tsx`.
10. **`app/services/[slug].tsx`** — لسه مفيش badge "موثّق" ولا chips
    الخدمات الفردية على كارت الشركة.

### تحسين مش نقص
**`(tabs)/account.tsx`** فيه زرار "تسجيل الخروج" مباشر وواضح — زيادة
مفيدة عن الويب، مش نقص.

---

## ملاحظة بنيوية (node_modules، مش جزء من الفيز نفسها)

أثناء تسطيب `react-native-markdown-display` (بند 11 فوق)، ظهرت مشكلة حقيقية
غير مرتبطة بمحتوى الفيز: `npm install` من الجذر كسر الـ typecheck تمامًا
(`@types/react` مالوش مكان ثابت يتلاقي منه، و`babel-preset-expo` كان
موجود بس معتمد على hoisting غير مضمون). اتصلحوا بإضافة الاتنين كـ
dependencies صريحة (`@types/react` على مستوى الـ root package.json،
`babel-preset-expo` جوه `mobile/client/package.json`) — إصلاح دائم مش
ترقيع، موثّق بكومنتات في الكود نفسه مفيهوش.

---

## لسه محتاج تجربة فعلية على محاكي (مش هيبان من الكود)

- إحساس اللمس/التمرير الفعلي (السرعة، الـ momentum، حجم لمس الأزرار).
- تلوين فعلي في الوضعين (مفيش dark mode في التطبيق أصلًا — تأكيد إن ده مقصود).
- سلوك لوحة المفاتيح مع كل فورم (خصوصًا `new-request` والـ Modal بتاعت الترتيب في `companies.tsx`).
- أداء الـ infinite scroll في `companies.tsx` و`services/[slug].tsx` مع قايمة طويلة فعلية.
- تجربة الإشعارات (push) الفعلية عند وصول رسالة.
- الـ deep link الحقيقي بعد ما يتربط الدومين (Phase 7).
- **الأهم دلوقتي: تجربة شاشة تأكيد السعر end-to-end** — خلّي بروفايدر يقفل
  طلب حقيقي بسعر نهائي، وتأكد إن العميل في التطبيق بيتقفل عليها فعليًا
  ومايقدرش يتخطاها. **ده لسه متجرّبش على جهاز حقيقي.**

---

**الحالة:** فيز 9 خلصت الـ 6 بنود المكتوبة في نصها + شاشة تأكيد السعر
(أولوية قصوى). قسم "ملاحظات متبقية" فوق فيه 10 بنود لسه مفتوحة عن قصد —
مش جزء من مهام فيز 9 المكتوبة، متسجّلة لفيز لاحقة. فيز 10 (كابتشا +
شاشة صيانة) موجودة بالفعل في `MOBILE-WEB-PARITY-PROMPT.md` ومنتظرة.
