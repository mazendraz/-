# برومبتات تنفيذ PLAN-V2

> **ماتديش البلان كلها في برومبت واحد.** ٨ مراحل (٠ … ٧)، كل واحدة migration
> منفصلة و ship منفصل. لو خلطتهم هتبوظ الريبو وهيبقى صعب ترجع.
> كل برومبت في محادثة جديدة.
>
> **النسخة دي محدّثة بعد جولتين مراجعة** — فيها قاعدة النشر (مسودة/منشور)،
> allowlist لكل كيان، partial unique index، شاشة crash صفر اعتماديات،
> `X-Lead-Token`، وتقديم تقسيم AdminDashboard قبل كل حاجة.

---

## 0️⃣ تجهيز اللوكال (مرة واحدة، قبل أي حاجة)

قاعدة البيانات المحلية فاضية — الـ volume اتعمل من جديد ولسه ما اتطبّقش عليه
أي migration. ده السبب إن `/api/companies` و `/api/settings` بيرجّعوا 500.

```
اتأكد إن api/.env فيه DATABASE_URL و DIRECT_URL على localhost:5433 (مش الإنتاج)
واطبعهم لي عشان أشوفهم بعيني قبل ما تكمل.

بعد التأكيد بس، شغّل npm run db:setup على اللوكال (generate + migrate + seed).
بعدها تأكد إن /api/companies و /api/settings و /api/ready كلهم بيرجّعوا 200،
وإن الموقع على :5173 بيعرض شركات فعلًا.

ماتعملش أي حاجة تانية.
```

---

## 🚀 برومبت البداية (لو بدأت محادثة جديدة ولسه ما قريتش البلان)

```
اقرأ CLAUDE.md و docs/plan-v2/README.md وكل ملفات docs/plan-v2/ بالكامل قبل ما تعمل أي حاجة.

بعد ما تقرأهم، اعمل التالي وبس:
1. لخّصلي في نقاط قصيرة فهمك للمراحل وترتيبها والاعتماديات بينها.
2. قوللي أي حاجة في البلان مش واضحة أو ليها أكتر من تفسير أو شايف إنها غلط
   معماريًا — من غير مجاملة.

ماتكتبش أي كود ولا تعمل أي migration دلوقتي. استنى موافقتي.
```

---

## المرحلة 0 — تقسيم AdminDashboard.tsx (قبل أي feature)

```
اقرأ CLAUDE.md الأول.

المهمة: قسّم app/src/pages/AdminDashboard.tsx على ملفات تحت
app/src/pages/admin/. (الملف حوالي 2400 سطر — اتأكد بنفسك، ماتعتمدش على الرقم.)

قيد صارم: ده ريفاكتور نقل ملفات فقط. صفر تغيير في السلوك، صفر تغيير في الـ
API، صفر تحسينات جانبية. أي حاجة تحس إنها محتاجة تتحسن اكتبهالي في قايمة
منفصلة وسيبها.

اعرض عليّ خطة التقسيم (أسماء الملفات وإيه اللي رايح فين) قبل ما تلمس أي كود.

لما تخلص: شغّل الأدمن داشبورد وعدّي على كل تبويب فيه وتأكد إن كل حاجة شغالة
زي الأول، وبعدين استنى موافقتي قبل الـ ship.
```

> ليه الأول وليه لوحدها: المرحلة 1 (Feature D) بتضيف تبويب "حالة الموقع" لنفس
> الملف — يعني لو أجّلت التقسيم هتكبّر الملف ثم تقسّمه. وكمان ريفاكتور بالحجم
> ده جوه PR فيه feature بيخلي الـ diff مستحيل يتراجع، ولو ظهرت مشكلة مش هتعرف
> هي من الريفاكتور ولا من الفيتشر.

---

## المرحلة 1 — صفحة الحالة / الصيانة

```
نفّذ المرحلة 1 من docs/plan-v2/phase-1-status-screen.md فقط.

اقرأ CLAUDE.md و docs/plan-v2/README.md الأول. المرحلة دي مالهاش migration خالص —
بتستعمل موديل AppSetting الموجود.

نقاط حرجة:
- 🔴 شاشة الـ crash لازم تكون ملف منفصل بصفر اعتماديات:
  CrashScreen.tsx بنصوص عربي/إنجليزي مدمجة كثوابت، اللغة من navigator.language،
  من غير t() ولا LocaleContext ولا أي fetch ولا قراءة إعدادات. السبب:
  ErrorBoundary مكانه فوق RouterProvider يعني فوق مصدر الـ context — لو اللي
  ضرب هو الـ locale provider أو جلب الإعدادات، شاشة الخطأ هترمي هي كمان
  وتدّي صفحة بيضا. StatusScreen يفضل للـ maintenance و offline ويقدر يبقى أغنى.
- 🔴 خطر الـ maintenance middleware معكوس: المشروع مافيهوش global middleware،
  دي wrappers بتتلفّ يدويًا لكل راوت (وproxy.ts على Edge فمايقدرش يقرا الإعداد
  من Prisma). فـ /api/auth/* مش هتتلفّ أصلًا. الخطر الحقيقي إنك تنسى تلفّ راوت
  كتابي جديد فيفضل مفتوح في الصيانة بصمت. اكتب تست بيعدّي على كل ملفات
  api/src/app/api/**/route.ts اللي بتصدّر POST/PATCH/PUT/DELETE وهي مش تحت
  admin/ ولا provider/ ولا auth/، ويتأكد إن كلها ملفوفة بـ withMaintenance.
- /api/status الجديد هو المصدر الوحيد لبيانات الصيانة، بـ no-store.
  ماتضيفش maintenance في /api/settings — بيستعمل okCached بـ s-maxage=60
  و swr=300 فالتشغيل ممكن يتأخر ٥ دقايق على الـ CDN.
- 🟡 لفحص صحة الباك إند استعمل /api/ready الموجود (بيعمل SELECT 1)، مش
  /api/health اللي بيرجّع ok ثابت. بس ماتعملش polling دوري — /api/ready
  بيضرب الـ DB. خلي api.ts يبعت CustomEvent عند تكرار الفشل، والـ hook يبدأ
  polling بعد أول فشل بس ويقف بعد أول نجاح.
- كل نص جديد في app/src/lib/i18n.ts بالعربي والإنجليزي (ما عدا CrashScreen).
- الشاشة مظبوطة RTL و LTR، وتحترم prefers-reduced-motion.

اشتغل على خطوات، وبعد كل خطوة كبيرة قوللي إيه اللي اتعمل.
لما تخلص، نفّذ تشيك ليست القسم 6 في docs/plan-v2/README.md وقوللي نتيجة كل بند، واستنى موافقتي قبل
الـ ship.
```

---

## المرحلة 2 — نظام الموافقات العام

```
نفّذ المرحلة 2 من docs/plan-v2/phase-2-change-requests.md فقط.

⚠ الموديل عام عن قصد واسمه ChangeRequest مش ProfileChangeRequest — المرحلة 3
(الأسعار) هتستعمله بنفسه ونفس شاشة المراجعة. ماتعملش نظام موافقة تاني بعدين.

نقاط حرجة:
- الـ migration على اللوكال بس (راجع قاعدة قاعدة البيانات في CLAUDE.md).
- 🔴 allowlist لكل كيان مش للشركة بس. الموافقة معناها
  prisma[entity].update({ data: changes }) و changes جاية من البروفيدر —
  من غير allowlist لكل من COMPANY و OFFERING و OFFERING_TIER و BUNDLE_RULE
  ده mass-assignment: يبعت { isPublished: true } أو { companyId: "..." }
  ويعدّي. راجع جدول EDITABLE_FIELDS في phase-2-change-requests.md والتزم بيه. تحقّق مرتين:
  وقت الإرسال ووقت الموافقة. واكتب تست بيبعت كل مفتاح ممنوع واحد واحد.
- 🔴 دمج مش استبدال: لو في طلب PENDING على نفس الكيان وبعت تعديل جديد، الطلب
  الجديد يبدأ من changes بتاع القديم ويعمل merge فوقه. سيناريو "عدّل phone
  وبعت، بعدين عدّل about وبعت" لازم ينتهي بطلب فيه الاتنين. تست إجباري —
  ضياع تعديل بصمت هنا كارثة ثقة.
- 🔴 قاعدة "PENDING واحد لكل (entity, entityId)" محتاجة قيد في قاعدة البيانات
  مش في الكود بس، وإلا دبل كليك بيعمل طلبين. Prisma مابيعرفش partial unique
  index — اكتبه SQL خام في ملف الـ migration:
  CREATE UNIQUE INDEX "change_request_one_pending"
    ON "ChangeRequest" (entity, "entityId") WHERE status = 'PENDING';
  وامسك خطأ التفرّد في السيرفس وحوّله لرسالة مفهومة مش 500.
- entityId إجباري (String مش String?) — للـ COMPANY = companyId نفسه. مفيش
  operation اسمها CREATE؛ العمليات: PUBLISH / UPDATE / DELETE.
- 🔴 كشف التعارض بمقارنة عميقة مش !== . gallery و badges مصفوفات، و
  ["a"] !== ["a"] بيرجّع true دايمًا — يعني تحذير كاذب في كل مراجعة فيها صور،
  والأدمن هيتعوّد يتجاهله خلال أسبوع. استعمل isDeepStrictEqual واعمل تست على
  مصفوفة بنفس المحتوى.
- الموافقة داخل transaction + AuditLog + إشعار للبروفيدر.
- في الـ UI اكتب صريح إن الموافقة الجزئية على مستوى الحقل كله — gallery إما
  كلها أو لا شيء.
- احذف رسالة "Profile information is managed by the Al Assema admin team" من
  ProviderDashboard.tsx.
- حدّث api/prisma/seed.ts بطلب معلّق واحد على الأقل للاختبار.

اكتب تستات للحالات المكتوبة في آخر phase-2-change-requests.md.
لما تخلص، تشيك ليست القسم 6 في docs/plan-v2/README.md واستنى موافقتي قبل الـ ship.
```

---

## المرحلة 3 — الأسعار · الجزء B1 (باك إند)

```
نفّذ الجزء B1 من docs/plan-v2/phase-3-offerings.md: الباك إند فقط.
يعني: السكيما + migration + سكربت الترحيل + السيرفس + الفاليديشن +
الـ endpoints + دمج طابور المراجعة في شاشة Feature A. من غير أي واجهات جديدة.

🔴 اقرأ "قاعدة النشر" في أول phase-3-offerings.md كويس قبل ما تكتب أي سكيما. القاعدة
الحاكمة في جملة واحدة:
  isPublished = false → مسودة يملكها البروفيدر → كتابة مباشرة على الصف
  isPublished = true  → محتوى عام → كل تعديل يمر بـ ChangeRequest
اللي بيحدد مسار الكتابة هو حالة الصف، مش نوع الـ endpoint. في phase-3-offerings.md جدول
كامل لكل حالة (POST / PATCH / DELETE / نشر) — التزم بيه حرفيًا.
اعمل الفرع ده في مكان واحد (assertWritePath) عشان ما يتكررش ويتنسى في endpoint.

نقاط حرجة تانية:
- استثناء صريح من قرار "كل حاجة تستنى موافقة": isActive و sortOrder بيتغيروا
  فورًا حتى على صف منشور، عبر endpoint منفصل (/visibility). السبب إن البروفيدر
  لازم يقدر يخفي عرض بسعر غلط دلوقتي. أي حاجة غير الاتنين دول → ChangeRequest.
  سجّلهم في AuditLog.
- أربع نماذج تسعير بالظبط: FIXED / RANGE / PER_UNIT / ON_INSPECTION.
  ON_INSPECTION ماتقبلش أي أرقام أسعار (zod refine). PER_UNIT لازم معاه unit.
- unit عبارة عن enum PriceUnit مش String حر. نص حر معناه "م²" و"متر مربع"
  و"m2" يبقوا وحدات مختلفة وده بيكسر مقارنة مرجع الفئة من أساسها.
- مفيش عمود currency. الجنيه المصري فقط.
- الشرائح (OfferingTier) ماتتداخلش في الكميات.
- مساعد الأدمن: مرجع الفئة (وسيط/أدنى/أعلى للعروض المنشورة بنفس PriceUnit)
  وتحذير لو السعر خارج ±60% من الوسيط. ماتعرضش المرجع ولا التحذير خالص لو
  عدد العروض في الفئة أقل من ٥ — اعرض "مفيش بيانات كافية للمقارنة".
- 🟠 سكربت الترحيل: idempotent عن طريق migratedFromService مش عن طريق الاسم.
  @@unique([companyId, migratedFromService]) — مش unique على name، ده كان
  هيمنع الشركة للأبد من إنها تعمل عرضين بنفس الاسم.
  و Company.services ممكن يكون فيها تكرار: اعمل de-dup صريح واطبع تحذير بكل
  تكرار، عشان upsert ما يسقطش خدمة بصمت. اطبع في الآخر: أُنشئ N · تُخطّي M ·
  تكرارات K. اختبره بتشغيلتين ورا بعض على اللوكال.
- سيب عمود Company.services موجود كـ fallback — ماتحذفوش دلوقتي.
- حدّث api/prisma/seed.ts بعروض بأربع النماذج + شرائح + قاعدة باقة.

لما تخلص، تشيك ليست القسم 6 في docs/plan-v2/README.md واستنى موافقتي قبل الـ ship.
```

## المرحلة 3 — الأسعار · الجزء B2 (واجهات)

```
نفّذ الجزء B2 من docs/plan-v2/phase-3-offerings.md: الواجهات فقط.
الباك إند خلص في B1 — ماتغيّرش فيه إلا لو لقيت باج حقيقي (وقتها قوللي الأول).

المطلوب:
- تبويب "الخدمات والأسعار" في ProviderDashboard: جدول العروض + محرر modal
  فيه اختيار النموذج والشرائح والوحدة والصورة.
- تمييز بصري واضح بين ٣ حالات: مسودة (بيعدّل فيها على طول) · منشورة ·
  منشورة وعليها تعديل تحت المراجعة. الحالة التالتة تعرض "المنشور: كذا |
  تحت المراجعة: كذا" — مش تخفي ولا تلخبط.
- زر إخفاء/إظهار سريع (isActive) مفصول وواضح إنه فوري من غير مراجعة.
- كروت الأسعار في CompanyProfile مقسومة خدمات/منتجات + شارة مميزة لـ
  "السعر يتحدد بعد المعاينة".
- دالة تنسيق واحدة app/src/lib/pricing.ts تستعملها كل الصفحات، بأرقام
  عربية/إنجليزية حسب اللغة.
- كل النصوص في i18n.ts بالعربي والإنجليزي، والكروت مظبوطة RTL.

لما تخلص، تشيك ليست القسم 6 في docs/plan-v2/README.md واستنى موافقتي قبل الـ ship.
```

---

## المرحلة 4 — طلب متعدد الخدمات

```
نفّذ المرحلة 4 من docs/plan-v2/phase-4-multi-service.md فقط.

نقاط حرجة:
- الأسعار بتتصوّر snapshot لحظة الطلب في LeadItem. تغيير سعر العرض بعدين
  ماينفعش يغيّر أي طلب قديم — تست إجباري.
- استبعد بنود ON_INSPECTION من الحساب صراحةً بـ filter على pricingModel،
  مش بالاعتماد على إن أسعارها null. راجع الصيغة المكتوبة في phase-4-multi-service.md بالظبط.
- لو كل البنود ON_INSPECTION: اعرض "السعر يتحدد بعد المعاينة" مش 0 – 0 ج.
  تست إجباري.
- الخصم: العتبة minItems بتتحسب على كل البنود المختارة، بس الخصم بينطبق على
  المبلغ المسعّر بس. اعرضه صريح: "خصم الباقة ١٥٪ (على البنود المسعّرة)".
  أعلى قاعدة منشورة ونشطة تنطبق، ومرة واحدة بس.
- منطق الحساب متكرر في api و app. اعمل pricing-cases.json في جذر الريبو
  وتستين بيقرأوه بـ fs.readFileSync وقت التست (مش import — كده مش محتاج
  tsconfig paths ولا إعداد vitest ولا server.fs.allow). الباك إند هو المرجع.
- التوافق الخلفي: خلي Lead.service يتملّى بأسماء البنود مفصولة بفاصلة عشان
  القوائم والإشعارات والتصدير القديمة ما تبوظش.
- الشريط العائم: اعمل app/src/components/RequestBar.tsx جديد واستعمل الكلاس
  compare-bar-offset الموجود في index.css (مفيش مكوّن CompareBar في المشروع).
- حدّث seed.ts بطلب أو اتنين فيهم LeadItem متعددة.

لما تخلص، تشيك ليست القسم 6 في docs/plan-v2/README.md واستنى موافقتي قبل الـ ship.
```

---

## المرحلة 5 — فترات الانشغال المجدولة

```
نفّذ المرحلة 5 من docs/plan-v2/phase-5-busy-windows.md فقط.

نقاط حرجة:
- دي إضافة فوق نظام busy/busyUntil الموجود، مش استبدال. AvailabilityControl
  والـ waitlist يفضلوا شغالين زي ما هما.
- 🟠 isEffectivelyBusy في serialize.ts — غيّر التوقيع لـ (c, windows)
  ببارامتر إجباري، من غير = [] . الديفولت الفاضي مش بيحمي الاستدعاءات
  القديمة، بيخليها غلط بصمت: أي مسار نسي يمرّر الفترات هيعرض شركة مشغولة
  على إنها متاحة والعميل يبعتلها طلب. خليه إجباري وسيب TypeScript يمسك
  كل الاستدعاءات وقت الـ build.
- 🟠 مين بيجيب الـ windows: serializeCompany بتتنادى لكل شركة في
  GET /api/companies (pageSize=100) — أسخن endpoint في المشروع. ممنوع
  استعلام لكل شركة. استعلام واحد للصفحة كلها بـ companyId: { in: ids } و
  OR: [{ endsAt: null }, { endsAt: { gt: now } }]، وتجميع في الذاكرة.
  راجع الكود المكتوب في phase-5-busy-windows.md.
- 🟠 الفترة المفتوحة (endsAt = null) بتتعارض مع منع التداخل. القاعدة:
  فترة مفتوحة واحدة كحد أقصى، وإنشاء واحدة جديدة بيقفل القديمة (endsAt = now)
  بدل ما يرفض. فحص التداخل بيعامل null كـ infinity. تست للتلات حالات.
- مفيش cron ولا أي job — كل حاجة تتحسب وقت القراءة.
- البروفيدر ماينفعش يحذف فترة عملها الأدمن (createdByAdmin) → 403.
- على البروفايل العام: شكل هادي مش مضايق — شارة صغيرة "مشغول · يرجع ١٢
  أغسطس" أو "متاح · بيرد خلال ساعتين". مش بانر كبير ولا مودال.
- حدّث seed.ts بفترة جارية وفترة جاية.

لما تخلص، تشيك ليست القسم 6 في docs/plan-v2/README.md واستنى موافقتي قبل الـ ship.
```

---

## المرحلة 6 — الشات

```
نفّذ المرحلة 6 من docs/plan-v2/phase-6-chat.md فقط.

دي أكبر مرحلة. قسّمها لخطوات واعرض عليّ الخطة التفصيلية قبل ما تبدأ.

نقاط حرجة (أمان):
- 🔴 التوكن في هيدر X-Lead-Token مش في الـ query string. ده مش نفس تعرّض
  بوابة التتبع: التتبع بيسجّل التوكن في لوجات Caddy مرة واحدة، والشات بـ
  polling كل ٨ ثواني بيسجّله مئات المرات لكل محادثة — تضخيم حقيقي في مساحة
  التعرّض (اللوجات، النسخ الاحتياطية، أي حد بيقرا اللوج). التكلفة سطرين وقت
  ما تكتب customerGuard.ts من الأصل.
- بوابة العميل: استعمل leadSecretMatches و trackByRefAndSecret الموجودين في
  api/src/lib/services/leads.service.ts. ماتكتبش المنطق ده من الأول.
- رقم طلب غلط أو توكن غلط → نفس الـ 404 بالظبط، من غير أي فرق في الرد ولا
  في وقت الرد.
- البروفيدر يفتح محادثة شركة تانية → 403.
- استعمل rateLimit و bodyLimit و sanitize الموجودين. حد أقصى 2000 حرف.
  rateLimit على الـ GET كمان مش على الـ POST بس.

الأداء (بالترتيب الصح):
1. الـ backoff هو التخفيف الحقيقي: ٨ ثواني وقت النشاط، تزيد لـ ٣٠ ثانية بعد
   دقيقتين سكوت، وترجع ٨ فور أي إرسال. يقف مع document.hidden.
2. ETag من lastMessageAt + رد 304. ده بيوفر استعلام الرسايل والنقل — مش
   بيوفر الاستعلام كله (لازم تجيب الـ Conversation عشان تحسب الـ ETag).
3. ماتعملش SSE ولا WebSocket دلوقتي.

نقاط تانية:
- المحادثة تتعمل تلقائيًا مع إنشاء الـ Lead في leads.service.ts.
- إشعارات البروفيدر مجمّعة debounce 60 ثانية — مش إشعار لكل رسالة.
- الأدمن: قراءة كل المحادثات + إرسال كـ ADMIN بعلامة "إدارة العاصمة" +
  إخفاء رسالة (تفضل ظاهرة له هو بس).
- حدّث seed.ts بمحادثة فيها رسايل من الطرفين.

اكتب تستات لكل حالات الأمان في آخر phase-6-chat.md.
لما تخلص، تشيك ليست القسم 6 في docs/plan-v2/README.md واستنى موافقتي قبل الـ ship.
```

---

## المرحلة 6.1 — تصحيح: الشات يبقى صفحة مستقلة (مش inline)

```
اقرأ "🔴 تصحيح — الشات لازم يبقى صفحة مستقلة" في آخر
docs/plan-v2/phase-6-chat.md ونفّذه بالظبط.

المشكلة: النسخة الحالية من الشات بتفتح ChatThread كـ accordion صغير جوه
كارت الطلب في MyRequests.tsx. المطلوب: صفحة مستقلة كاملة الشاشة على راوت
/requests/:refNumber/chat.

نقاط حرجة:
- التوكن يتحل من useMyLeads() (نفس الهوك اللي MyRequests.tsx شغّال بيه
  دلوقتي، بيقرا من localStorage) — مش من الـ URL. الـ refNumber في الـ path
  بس، والتوكن يتجاب من نتيجة useMyLeads بالبحث بالـ refNumber. ماتحطش
  trackingToken في الرابط ولا كـ query param.
- لو الطلب مش موجود في localStorage الجهاز ده، اعرض صفحة "الطلب مش موجود
  على الجهاز ده" مع زر رجوع لـ /requests — من غير أي fallback يحل التوكن من
  مكان تاني.
- ChatThread.tsx نفسه ما يتغيّرش — بس مكان استخدامه.
- في MyRequests.tsx: امسح حالة chatting والعرض الـ inline بالكامل، وحوّل
  الزر لـ Link عادي.
- راوت جديد lazy-loaded في main.tsx زي باقي الراوتات.
- صفحة RequestChat.tsx: هيدر فيه اسم الشركة ورقم الطلب + زر رجوع، والمحتوى
  كامل الشاشة مش مربع صغير.
- كل نص جديد في i18n.ts بالعربي والإنجليزي، ومظبوط RTL.

لما تخلص: افتح /requests وجرّب تدخل شات لطلب حقيقي، واتأكد إن الرابط ما فيهوش
التوكن، وإن F5 على صفحة الشات شغّال عادي. استنى موافقتي قبل الـ ship.
```

---

## المرحلة 7A — الترجمة: الإصلاحات اللي العميل بيشوفها

```
نفّذ الجزء 7A من docs/plan-v2/phase-7-i18n.md فقط. اقرأ الملف الأول.

النطاق: المكونات العامة المكسورة + الباجات المحددة + نقل الـ ternary العامة
+ حارس ضد التراجع. ماتلمسش لوحة الأدمن ولا لوحة البروفيدر — دول 7B و 7C.

قيد صارم: ده نقل نصوص. صفر تغيير في السلوك، صفر تحسينات جانبية. أي باج
تلاقيه اكتبه في قايمة منفصلة وسيبه.

🔴 CrashScreen.tsx يفضل زي ما هو بالظبط. نصوصه inline عن قصد لأنه لازم يبقى
بصفر اعتماديات — ماتنقلهوش لـ i18n.ts ولا تخليه يستدعي t(). حط تعليق جنبه
يمنع أي حد "يصلّحه" بعدين.

خليك مركّز على: SaveButton (زر موجود على كل كرت شركة) · AuthGate ·
SearchInput و Pagination (aria-labels — قارئ الشاشة) · Captcha · CatalogError
· ونقل ChatThread و RequestItemPicker و RequestBar و OfferingCards لـ t().

والباجات: دالة formatDate/formatDateTime موحدة تاخد locale (التواريخ دلوقتي
بتاخد لغة الجهاز مش لغة الموقع) · formatReopenDate تبقى locale إجباري ·
توحيد "Al Assemah" مع "Al Assema" · شيل .toLowerCase() من نص مترجم في
RequestForm.

وفي الآخر اكتب الحارس: تست بيمشي على app/src/**/*.tsx ويوقع على أي نص JSX
حرفي لاتيني أو placeholder/aria-label/title حرفي أو toLocaleDateString من
غير locale — مع قايمة استثناءات صريحة ومعلّلة (CrashScreen + الملفات اللي
لسه ما اتنقلتش في 7B/7C).

لما تخلص: بدّل اللغة في المتصفح وعدّي على كل الصفحات العامة، ودوّر على نص
إنجليزي فاضل أو تخطيط اتكسر في RTL. وبعدين استنى موافقتي قبل الـ ship.
```

---

## المرحلة 7B — الترجمة: لوحة البروفيدر

```
نفّذ الجزء 7B من docs/plan-v2/phase-7-i18n.md فقط. اقرأ الملف الأول.

النطاق: ProviderDashboard.tsx + ProfileEditor + OfferingsEditor +
BusyWindowsEditor + ProviderChat + AvailabilityControl + WaitlistManager +
TelegramConnect + NotificationToggle. حوالي ١١١ مفتاح جديد بـ prefix prov_*.

ماتلمسش لوحة الأدمن — دي 7C.

قواعد: المفتاح يتضاف في en و ar في نفس التعديل وبنفس الترتيب. صفر تغيير في
السلوك. شيل الملفات اللي خلصتها من قايمة استثناءات الحارس اللي اتكتب في 7A.

لما تخلص: ادخل بحساب بروفيدر، بدّل اللغة، وعدّي على كل تبويب. استنى موافقتي
قبل الـ ship.
```

---

## المرحلة 7C — الترجمة: لوحة الأدمن

```
نفّذ الجزء 7C من docs/plan-v2/phase-7-i18n.md فقط. اقرأ الملف الأول.

النطاق: كل ملفات pages/admin/ (١٧ ملف). حوالي ١٧٠ مفتاح جديد بـ prefix admin_*.
ده أكبر جزء — قسّمه لخطوات (تبويب تبويب) وقوللي بعد كل تبويب.

قواعد: المفتاح يتضاف في en و ar في نفس التعديل وبنفس الترتيب. صفر تغيير في
السلوك. لما تخلص، قايمة استثناءات الحارس المفروض تفضل فيها CrashScreen بس.

لما تخلص: ادخل بحساب أدمن، بدّل اللغة، وعدّي على كل تبويب وكل مودال.
استنى موافقتي قبل الـ ship.
```

---

## المرحلة 8 — برومبت واحد، ٤ تصحيحات (بالترتيب ده بالظبط)

> ده الاستثناء الوحيد من قاعدة "برومبت واحد لكل مرحلة" — الأربع تصحيحات دول
> صغار ومترابطين في نفس الجلسة (شات، بيزي، أسعار، فيديو)، فمقبول تديهم مرة
> واحدة. **بس خلّي كلود كود يعمل ship بعد كل جزء لوحده** — مش ship واحد
> للأربعة مع بعض، عشان لو حاجة بوظت تقدر ترجعها لوحدها.

```
اقرأ docs/plan-v2/phase-8-fixes.md بالكامل الأول — خصوصًا "0. معيار الجودة"
في الأول، لأنه بينطبق على الأربعة أجزاء وليس اختياريًا.

🔴 شرط قبل أي ship في الأربعة: الديزاين يستعمل design tokens الموجودة فعلًا
في app/tailwind.config.js و app/src/index.css (bg-surface-container-lowest،
rounded-2xl، shadow-bloom، font-headline-md، touch-press/btn-press، مكوّن
EmptyState الموجود) — ممنوع تخترع ألوان أو مقاسات جديدة. وكل شاشة جديدة أو
معدّلة لازم فيها الحالات الأربعة: تحميل / فاضي / خطأ / نجاح، مش الحالة
السعيدة بس. وقبل ما تقول "خلصت" في أي جزء، نفّذ تشيك ليست "صفر باجات" في
آخر قسم "0" حرفيًا (عربي وإنجليزي، موبايل وديسكتوب، console بلا أخطاء،
دبل-كليك ما بيكررش الفعل، البيانات بتتحدث فعليًا بعد الحفظ). لو بند فشل،
الجزء ده لسه مش خالص، حتى لو باقي الأجزاء الأربعة تمام.

رتّب الأربعة بالأولوية دي بالظبط ولا تخلطهم مع بعض:

═══ 8.1 — الأدمن والشات ═══
شخّص الأول قبل ما تعدّل أي كود: شغّل npm run db:setup على اللوكال (لو مش
متشغّل)، أعد تشغيل npm run dev:api و npm run dev:app، وادخل /admin وجرّب
تبويب "المحادثات". لو اشتغل بعد كده، وثّق إن المشكلة كانت بيئة بس ومتعملش
تغيير في الكود غير الـ badge تحت. لو لسه مش شغّال، شخّص من الأخطاء الفعلية
(network tab / server logs) وقولّي قبل ما تصلّح أي حاجة.
بعد كده بس: ضيف badge بعدد المحادثات غير المقروءة جنب تبويب "المحادثات" في
nav.ts، بنفس نمط badge الـ changes الموجود.
ship لوحده.

═══ 8.2 — 🔴 باج حقيقي: مصدرين للبيزي ═══
ده مش تحسين شكلي، ده باج: الزرار السريع في قائمة الشركات (admin/index.tsx)
بيغيّر Company.busy بس، من غير ما يلمس BusyWindow — فلو في فترة مجدولة
شغّالة، الزرار "بيبان إنه ما عملش حاجة" لأنه فعليًا كده.
الحل: شيل الزرار السريع، وخلي الضغط بدل منه يفتح CompanyEditor على تبويب
الإتاحة مباشرة. وحّد AvailabilityControl و BusyWindowsEditor بصريًا في شاشة
واحدة بمنطق واضح: سطر حالة واحد فوق (متاح الآن / مشغول الآن - والسبب)، زرار
واحد بس للتبديل الفوري، وقسم منفصل تحته لأي جدولة مسبقة. طبّق نفس التوحيد في
ProviderDashboard.tsx (نفس التكرار موجود هناك). بادج "مشغول" في قائمة
الشركات يوضّح المصدر (يدوي / فترة مجدولة لحد كذا).
لما تخلص جرّب: شركة عليها فترة مجدولة شغّالة، اتأكد إن الشاشة بتوضّح ده
واضح وإن مفيش زرار تاني بيتجاهله. ship لوحده.

═══ 8.3 — تبسيط شاشة الأسعار (الأكبر) ═══
البنية تحت (draft/published + ChangeRequest + tiers) متتغيرش خالص — ده
تبسيط في العرض والـ copy بس، مش إعادة بناء. اقرأ القسم كامل في
phase-8-fixes.md والتزم بالتصميم المكتوب فيه بالظبط:
- فورم إضافة عرض بسؤالين بس بلغة يومية (إيه اللي بتبيعه؟ السعر إزاي؟) مع
  كروت اختيار عليها أمثلة حقيقية (باب مصفح، شقة مفروشة) بدل dropdown تقني.
- الشرائح (tiers) تختفي من الفورم الأساسي وتتحط خلف رابط ثانوي "عايز أسعار
  مختلفة حسب الحجم؟ (اختياري)".
- حالة المراجعة تتحول من ٤ badges لجملة واحدة مفهومة تحت اسم العرض، وزرار
  فعل أساسي واحد واضح بدل ٤ أزرار متجاورة (تعديل/حذف في قائمة نقط ⋮ صغيرة).
- سيب رابط "وضع متقدم" في الآخر يرجّع للفورم الكامل الحالي لمين عايزه.
قسّمها لخطوات (الفورم البسيط، إخفاء الشرائح، تبسيط الحالة) وقوللي بعد كل
خطوة. لما تخلص جرّب تضيف "باب مصفح" بسعر ثابت من الفورم البسيط في أقل من
٣٠ ثانية من غير ما تفتح "وضع متقدم". ship لوحده.

═══ 8.4 — رفع فيديو ═══
upload.service.ts دلوقتي كله sharp (مكتبة صور) و ALLOWED_MIME بيقبل صور بس
— الفيديو مش باج، هو ميزة مش موجودة خالص. اقرأ القسم في phase-8-fixes.md:
ضيف video/mp4 و video/webm للـ allowlist لكن افصل مسارهم عن sharp تمامًا
(تخطي processImage بالكامل لو الملف فيديو)، وحد أقصى أكبر من الصور (٣٠MB
مقترح). في الفرونت: accept على كل input رفع معرض/مشاريع يقبل الفيديو كمان،
واعمل helper isVideoUrl() واحد في app/src/lib/image.ts يتستعمل في كل مكان
بيعرض معرض أو مشاريع عشان يرندر <video controls> بدل <img> لما الرابط
فيديو. الـ lightbox في CompanyProfile.tsx يشتغل مع الفيديو كمان (بلا
autoplay). اتأكد إن رفع الصور العادي لسه شغال زي الأول من غير أي كسر.
ship لوحده.

بعد الأربعة: شغّل تشيك ليست القسم 6 في docs/plan-v2/README.md، وتشيك ليست
"صفر باجات" في قسم "0" من phase-8-fixes.md، مرة أخيرة على الكل. قوللي
الحالة النهائية لكل جزء من الأربعة — ولو أي بند من تشيك ليست الباجات فاشل
في أي جزء، قوله صراحة بدل ما تعتبره خالص.
```

---

## المرحلة 9 — وضع التسعير على مستوى الفئة (Category Pricing Mode)

> محتاجة Phase 3 (الأسعار) خلصانة الأول. جزئين — كل واحد ship لوحده.

```
اقرأ docs/plan-v2/phase-9-category-pricing-mode.md بالكامل الأول.

نفّذ الجزء ده بس: الباك إند + الأدمن (الأقسام 1، 2، 3).

- migration واحدة إضافية: enum CategoryPricingMode { QUOTE_ONLY FIXED_CATALOG }
  + عمود pricingMode على Category بـ default QUOTE_ONLY. صفر تأثير رجعي —
  كل الفئات الموجودة تفضل QUOTE_ONLY زي ما هي، والفرونت الحالي (chips بلا
  أسعار) يفضل شغال لأنه fallback أصلًا موجود.
- 🔴 الحماية الحقيقية في offerings.service.ts: أي إنشاء/تعديل/نشر لـ
  Offering أو OfferingTier أو BundleRule لازم يتأكد إن
  company.category.pricingMode === "FIXED_CATALOG" ويرفض لو لأ. إخفاء
  التبويب في الواجهة وحده مش حماية — لازم تحقّق فعلي في السيرفر.
- ضيف pricingMode لـ ApiCategory و categoryPricingMode لـ ApiCompany في
  الاتنين (api/src/lib/apiTypes.ts و app/src/lib/apiTypes.ts)، محسوبة
  سيرفر-سايد في companies.service.ts عشان الفرونت ميحتاجش نداء إضافي.
- في CategoryEditor.tsx (الأدمن): اختيار بسيط/كتالوج أسعار بنفس نمط الحقول
  الموجودة في الملف، مع سطر توضيحي تحته. لو الفئة فيها شركات عندها
  Offerings منشورة وحاولت تعطّل الكتالوج، اعرض تحذير بعدد الشركات المتأثرة
  (تأكيد بس، مش منع من التبديل).

استعمل نفس design tokens ومعيار الجودة المكتوب في phase-8-fixes.md قسم "0"
(الحالات الأربعة: تحميل/فاضي/خطأ/نجاح، RTL وLTR، صفر أخطاء console).

لما تخلص، جرّب: فئة QUOTE_ONLY فيها شركة تحاول تعمل Offering عبر الـ API
مباشرة → ترفض. فعّل فئة FIXED_CATALOG وبدّلها تاني → تحذير العدد ظهر صح.
تشيك ليست القسم 6 في docs/plan-v2/README.md، واستنى موافقتي قبل الـ ship.
```

```
نفّذ الجزء التاني من phase-9-category-pricing-mode.md: الفرونت (الأقسام 4، 5).
الباك إند خلص في الجزء اللي قبل ده — ماتغيّرش فيه إلا لو لقيت باج حقيقي.

في ProviderDashboard.tsx: TAB_CONFIG (سطر ~37) لازم تتفلتر — تبويب "pricing"
يظهر بس لو company.categoryPricingMode === "FIXED_CATALOG". لو التبويب
المختار حاليًا اختفى من القايمة (الأدمن عطّل الفئة والبروفيدر فاتح
التبويب) ارجعه لـ overview تلقائيًا من غير شاشة بيضا. ولو حد وصل بـ deep
link لتبويب مش متاح، اعرض EmptyState بدل الفراغ.

🔴 الجزء الأهم — توحيد مسار الطلب في CompanyProfile.tsx: فيه ٤ نسخ منفصلة
من زرار "اطلب الخدمة" (هيدر ديسكتوب، هيدر موبايل، sidebar تبويب نظرة عامة،
أسفل تبويب المشاريع) وكلهم بيوصلوا لفورم فاضي متخطّيين كروت الأسعار خالص.
اعمل مكوّن واحد مشترك PricingCTA يستبدل الأربعة، وسلوكه لما الشركة
FIXED_CATALOG: يتحول النص لـ "اختر الخدمات"، والدوسة تحول التبويب لـ
Overview لو مش مفتوح، تعمل smooth scroll لقسم كروت الأسعار (حط
id="pricing-section" على القسم ده مع scroll-margin-top مناسب)، وهالة
تمييز بصري خفيفة تختفي بعد ثانية تقريبًا (تتخطى مع prefers-reduced-motion).
لما الشركة QUOTE_ONLY، PricingCTA يتصرف زي الأربعة زرارات القديمة بالظبط —
Link مباشر للفورم، صفر فرق. حالة busy (قائمة الانتظار) ما تتأثرش خالص.

تحت كروت الأسعار مباشرة، رابط نصي خفيف الوزن (مش زرار) "طلبك مش من ضمن
دول؟ اطلب طلب مخصص" يودي على طول للفورم الفاضي — يظهر بس في FIXED_CATALOG
ومرة واحدة، عشان العميل اللي طلبه خاص يفضل عنده مخرج من غير ما ينافس بصريًا
المسار الأساسي.

RequestForm.tsx ماتلمسهوش — أصلًا بيقرا من السلة لو موجودة ويرجع للسيلكت
القديم لو مفيش عروض.

نفّذ تشيك ليست القسم 6 كامل في phase-9-category-pricing-mode.md حرفيًا
(فئة QUOTE_ONLY مطابقة حرفيًا للشكل القديم، الأربع أماكن كلهم بيوصلوا
لنفس النتيجة في FIXED_CATALOG، RTL/LTR، تبديل الفئة رجوعًا وقدامًا ما
بيحذفش أي Offering موجود). كل نص جديد في i18n.ts بالعربي والإنجليزي.
استنى موافقتي قبل الـ ship.
```

---

## المرحلة 10 — تحديث بصري لمسار الطلب (Requests flow)

> ريفاكتور بصري/UX فقط. صفر migration، صفر تغيير في منطق الحساب. آمن يتعمل
> في أي وقت بعد المرحلة 4 (طلب متعدد الخدمات) لأنه بيلمس نفس الملفات.

```
اقرأ docs/plan-v2/phase-10-requests-visual-refresh.md بالكامل الأول، وكل
الملفات المذكورة في قسم 0 منه كاملة قبل ما تلمس أي كود.

نفّذ الأقسام 1 إلى 5 بالترتيب:
1. باج حقيقي: RequestItemPicker.tsx سطر 72 فيه
   accent-[color:var(--color-primary,#8a6a4f)] — المتغيّر ده مش متعرّف في
   أي مكان، فالـ checkbox بيطلع دايمًا باللون الاحتياطي البني بدل الأزرق
   الأساسي الحقيقي #005578. هيتصلح تلقائيًا لما تستبدل الـ checkbox
   بالمكوّن المخصص في القسم 2.
2. RequestItemPicker.tsx: استبدال الـ checkbox الأصلي بمؤشر دائري مخصص
   (مع role="checkbox" و aria-checked ومنطقة لمس ٤٤px فعلية على
   الموبايل)، رفع radius الصف لـ rounded-2xl، عداد الكمية يبقى pill بأزرار
   ٣٦px بدل الأزرار الصغيرة الحالية، فصل صف الكمية/الخيار لسطرين منفصلين
   تحت breakpoint sm على الموبايل، وصندوق الإجمالي ياخد حد علوي بلون
   primary ورقم أكبر (20px، primary، بدل on-surface العادي).
3. RequestBar.tsx: لمسة واحدة بس — استبدل قيمة الظل المكتوبة يدويًا بـ
   shadow-bloom (نفس الـ token المستخدم في باقي الموقع).
4. RequestForm.tsx: وحّد صناديق التنبيه الأربعة (busy/prefill/trust/error)
   في مكوّن Notice واحد بأربع variants — النص والمنطق الشرطي يفضلوا زي ما
   هم بالظبط، الحاوية بس اللي بتتوحد.
5. MyRequests.tsx: ارفع رقم الإجمالي في كارت الطلب (lead.items) لنفس وزن
   العرض الجديد (primary + font-black) عشان يبقى متسق مع الرقم في
   RequestItemPicker.

قيد صارم: صفر نص جديد بدون ما يدخل i18n.ts بالعربي والإنجليزي (مفيش نص
مباشر جوه الكومبوننت). استخدم design tokens الموجودة بس (rounded-2xl،
shadow-bloom، إلخ) — ممنوع تخترع ألوان أو مقاسات جديدة، حسب معيار الجودة
في phase-8-fixes.md قسم "0".

لما تخلص، نفّذ تشيك ليست القسم 8 في phase-10-requests-visual-refresh.md
حرفيًا (خاص بالباج، منطقة اللمس، الاتساق بين الصفحات الثلاث). عربي
وإنجليزي، موبايل ٣٧٥px وديسكتوب، صفر console errors. استنى موافقتي قبل
الـ ship.
```

---

## 🔁 برومبتات مساعدة

**لو وقف في النص أو المحادثة طولت:**
```
اقرأ CLAUDE.md و docs/plan-v2/README.md، وبعدين اعمل git status و git diff عشان تعرف
إيه اللي اتعمل لحد دلوقتي. قوللي إحنا فين في المرحلة رقم [X] وإيه الباقي،
قبل ما تكمل.
```

**قبل أي migration على الإنتاج:**
```
اقرأ قسم الترحيل والرجوع في docs/plan-v2/README.md. خد نسخة احتياطية بـ pg_dump
الأول وقوللي مسارها، وأكّدلي إن الـ migration دي additive بالكامل (جداول
وأعمدة جديدة بس — مفيش حذف عمود ولا تغيير نوع ولا NOT NULL على عمود قايم).
لو فيها أي حاجة غير additive، قف وقوللي.
```

**قبل أي ship:**
```
شغّل تشيك ليست القسم 6 من docs/plan-v2/README.md بند بند وقوللي نتيجة كل واحد.
لو كله تمام، ارفع بـ npm run ship -- "وصف مختصر للتعديل".
ماتعملش rebase ولا push --force ولا branch جديد.
```

**لو git اتلخبط (قفل عالق):**
```
git بيتلخبط أحيانًا من قفل .git/index.lock عالق (VS Code أو الأنتي فيروس).
اقرأ قسم «مكان الريبو + أقفال git» في CLAUDE.md واتبعه بالظبط: امسح
.git/index.lock وبعدين git reset، وبعدها git status عشان تتأكد إن كل حاجة
تمام. ماتعملش أي أمر تاني.
```
