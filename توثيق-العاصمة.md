# توثيق مشروع العاصمة (Al Assema)

> تاريخ الإعداد: 12 أغسطس 2026
> الغرض: فهم تقني وإداري كامل للمشروع الحالي، تمهيدًا لبناء أبلكيشن يدير الحسابات والمحاسبة والإدارة.

---

## 1) إيه هو المشروع أصلاً

العاصمة منصة **دليل خدمات (lead generation)** للعاصمة الإدارية الجديدة. بتربط عملاء بمقدّمي خدمات (تشطيبات، ديكور، مقاولات...) — العميل يتصفح الشركات، يبعت طلب (Lead)، والشركة تتواصل معاه **بره المنصة** (تليفون/واتساب). مفيهاش مزايدات ولا مدفوعات جوه المنصة نفسها.

المشروع Monorepo فيه جزئين مستقلين:

| المجلد | إيه هو |
|---|---|
| `api/` | الباك إند — Next.js API Routes + Prisma 7 + Postgres (Supabase) |
| `app/` | الواجهة — Vite + React 18 + TypeScript + Tailwind + React Router |
| `deploy/` | سكربتات نشر VPS واحد خلف Caddy + PM2 |
| `docs/` | كل التوثيق التقني والخطط |

**الحالة**: المشروع شغّال ومنشور فعليًا على `alassema.tech` (مش تجربة/Prototype).

---

## 2) الـ Stack بالتفصيل

| الطبقة | التقنية |
|---|---|
| Frontend | Vite · React 18 · TypeScript · Tailwind CSS · React Router |
| Backend | Next.js 16 API Routes · Prisma 7 · Zod للتحقق من المدخلات |
| Database | PostgreSQL عبر Supabase |
| Storage | Supabase Storage (صور اللوجوهات، الغلاف، المعرض) |
| الإشعارات | Web Push + بوت Telegram (للأدمن ومقدمي الخدمة) |
| Infra | Caddy (HTTPS تلقائي) + PM2 على VPS واحد، نفس الدومين لكل من الواجهة والـ API |
| الاختبارات | Vitest (api) · Playwright (app e2e) |

**نقطة مهمة معمارياً**: الواجهة والـ API على نفس الدومين (`alassema.tech/` للواجهة، `alassema.tech/api/*` للباك إند) — مفيش CORS معقّد، والـ API بيتعامل زي أي REST API عادي بيرجّع JSON.

---

## 3) نموذج البيانات (Database Schema)

القاعدة فيها الموديلات دي (Prisma):

### الكيانات الأساسية
- **Company** — بيانات الشركة الكاملة: اسم، وصف، لوجو، غلاف، صور معرض، تليفون، إيميل، واتساب، موقع، سنين خبرة، badges، تقييم (rating) محسوب أو يدوي، حالة (ACTIVE/INACTIVE/SUSPENDED)، وحالة "مشغول" (busy) بتاريخ رجوع تلقائي.
- **Category** — تصنيفات الخدمات (تشطيبات، ديكور...)، لكل تصنيف وضع تسعير: QUOTE_ONLY (عرض سعر بس) أو FIXED_CATALOG (كتالوج أسعار ثابت).
- **CompanyCategory** — ربط شركة بتصنيفات متعددة (many-to-many)، فيه تصنيف "أساسي" واحد لكل شركة.
- **Lead** — الطلب اللي بيبعته العميل: اسم، تليفون، حي، ميزانية، وصف، حالة (New/Contacted/In Progress/Completed/Cancelled)، رقم مرجعي فريد، وتقدير سعر مأخوذ وقت الإرسال (snapshot، مش بيتغيّر لو السعر اتغيّر بعدين).
- **LeadItem** — بنود الطلب لو كان multi-item (خدمات متعددة في طلب واحد)، بره snapshot لسعرها وقت الطلب.
- **Review** — تقييمات العملاء، إما من عميل حقيقي بعد إكمال طلب (verified) أو مُنسّقة يدويًا من الأدمن، وفيها موافقة (approved) قبل الظهور.
- **Project** — أعمال سابقة (portfolio) للشركة، بحالة موافقة (Pending/Approved/Rejected).
- **User** — حسابات الدخول: دور ADMIN أو PROVIDER، مرتبط بشركة (للـ Provider).
- **Offering / OfferingTier** — كتالوج أسعار مقدّم الخدمة (خدمة/منتج بسعر ثابت أو نطاق أو بالوحدة)، بنظام مسودة/نشر.
- **BundleRule** — خصومات عند تجميع عدة بنود في طلب واحد.
- **WaitlistEntry** — قائمة انتظار لما شركة تبقى "مشغولة".
- **ChangeRequest** — نظام موافقات عام: أي تعديل مهم من مقدّم الخدمة (على بروفايله أو أسعاره) بيتحول لطلب تعديل يوافق/يرفضه الأدمن.
- **Conversation / Message** — شات بين العميل ومقدّم الخدمة (بإشراف الأدمن)، مربوط بكل Lead.
- **BusyWindow** — جدولة فترات "مشغول" بدون تدخل يدوي كل مرة.
- **Feedback** — رسائل زوار (شكوى/اقتراح/استفسار) على صفحة شركة معينة.
- **SiteReview** — تقييمات عامة عن المنصة نفسها (مش عن شركة).
- **AppSetting** — إعدادات عامة (مفتاح/قيمة)، زي تفعيل تقييمات الموقع.
- **AuditLog** — سجل تدقيق لكل فعل حساس (حذف، تغيير حالة، تعديل صلاحيات).
- **PushSubscription** — اشتراكات Web Push لكل مستخدم/جهاز.

### 🔴 ملاحظة جوهرية: مفيش أي نموذج مالي/محاسبي في المشروع الحالي

بعد مراجعة الـ schema بالكامل، **لا يوجد** أي موديل لـ: فواتير، مدفوعات، اشتراكات، عمولات، إيصالات، أو أي بيانات مالية. المنصة مصممة لتوليد Leads فقط — أي تعامل مالي بين العميل والشركة بيحصل **بره المنصة بالكامل**. ده بيأكد إن أي نظام "حسابات ومحاسبة" هيبقى **مكوّن جديد بالكامل**، مش تطوير على حاجة موجودة.

---

## 4) الأدوار والصلاحيات

فيه دورين بس حاليًا:

- **ADMIN** — صلاحية كاملة على كل شيء (شركات، طلبات، مستخدمين، إعدادات، موافقات).
- **PROVIDER** — مرتبط بشركة واحدة بس، شايف ويتحكم في بيانات شركته فقط (Ownership guard بيتأكد إن الـ Provider مايلمسش بيانات شركة تانية).

المصادقة (Auth) عن طريق Bearer token، والتحقق من الدور بيحصل على مستوى كل route بالباك إند (`withRole`, `assertOwnership`). فيه كمان Audit Log بيسجّل كل فعل حساس مع هوية الشخص اللي عمله.

---

## 5) لوحة تحكم الأدمن — جرد كامل (موجود فعليًا)

اللوحة (`/admin`) فيها التابات دي:

| التاب | الوظيفة |
|---|---|
| **نظرة عامة (Overview)** | إحصائيات عامة: عدد الطلبات، معدل التحويل، اتجاهات يومية/شهرية (Cairo timezone)، أفضل الشركات |
| **الطلبات (Leads)** | كل الطلبات من كل الشركات، فلترة وتغيير حالة |
| **الشركات (Companies)** | إضافة/تعديل/حذف شركة، تفعيل/تعليق، إدارة التوفر (busy)، صور المعرض، مشاريعها، تقييماتها، قائمة الانتظار، الكتالوج السعري |
| **الخدمات (Services/Categories)** | إدارة التصنيفات، تفعيلها، وضع التسعير لكل تصنيف |
| **الفريق (Team)** | إدارة حسابات المستخدمين (أدمن/مقدّمي خدمة)، تفعيل/تعطيل حساب |
| **التقييمات (Reviews)** | موافقة/رفض تقييمات العملاء، وتقييمات الموقع العامة |
| **طلبات التعديل (Change Requests)** | مراجعة والموافقة/الرفض على تعديلات مقدّمي الخدمة على بروفايلاتهم وأسعارهم |
| **المحادثات (Chat)** | إشراف على الشات بين العملاء ومقدّمي الخدمة، إخفاء رسائل مخالفة |
| **حالة الموقع (Site Status)** | وضع الصيانة (Maintenance mode) |
| **الإعدادات (Settings)** | إعدادات عامة، قوالب إيميل، إشعارات، ربط Telegram، رفع ملفات |

بالإضافة لسجل تدقيق (Audit Log) منفصل بيسجل كل فعل إداري حساس.

---

## 6) لوحة تحكم مقدّم الخدمة — جرد كامل (موجود فعليًا)

اللوحة (`/provider`) فيها التابات دي:

| التاب | الوظيفة |
|---|---|
| **نظرة عامة** | إحصائيات الشركة الخاصة بيه |
| **الطلبات (Leads)** | الطلبات الجاية لشركته، تغيير حالتها |
| **الرسايل (Messages)** | شات مباشر مع العملاء لكل طلب |
| **المشاريع (Projects)** | إضافة أعمال سابقة (تدخل مراجعة الأدمن) |
| **التقييمات (Reviews)** | عرض تقييمات عملائه |
| **التحليلات (Analytics)** | رسوم بيانية لأداء شركته |
| **الإتاحة (Availability)** | تفعيل/إلغاء وضع "مشغول"، جدولة فترات مستقبلية |
| **الخدمات والأسعار (Pricing)** | كتالوج الأسعار (لو تصنيفه بيسمح بيه)، شرائح كمية، خصومات تجميع |
| **البروفايل (Profile)** | بيانات الشركة العامة — أي تعديل مهم يتحول لطلب موافقة |
| **الإعدادات** | ربط Telegram، إشعارات |

---

## 7) خلاصة الفجوة: إيه اللي مش موجود ولازم يتبني من الصفر

بناءً على الجرد أعلاه، مفيش حاليًا أي حاجة من دول:

- فواتير أو سندات قبض/صرف
- تتبّع مدفوعات أو تحصيل
- حسابات (Chart of Accounts)، قيود يومية، ميزانيات
- عمولات المنصة على الشركات (subscription/commission)
- تقارير مالية (أرباح وخسائر، تدفقات نقدية)
- ربط ببوابات دفع
- إدارة موظفين/رواتب

يعني أي أبلكيشن "يدير الحسابات والمحاسبة" هيبقى **نظام مستقل جديد**، إما:
- **(أ) موديول جديد جوه نفس الـ `api/`** بيستخدم نفس الـ Prisma/Postgres ونفس نظام الأدوار (Admin/Provider)، أو
- **(ب) أبلكيشن منفصل تمامًا** بيتكامل مع بيانات العاصمة (شركات، طلبات) عن طريق الـ API الموجود.

---

## 8) مواصفات مقترحة (Spec) لأبلكيشن إدارة الحسابات والمحاسبة

### 8.1 الهدف
نظام يدير الجانب المالي والإداري لمنصة العاصمة — سواء محاسبة العملية التشغيلية للمنصة نفسها (لو هتاخد عمولة من الشركات)، أو محاسبة داخلية لإدارة المصاريف والإيرادات، أو الاتنين.

### 8.2 الأسئلة اللي محتاجة إجابة قبل البناء (مهم جدًا)
لسه فيه قرارات عمل (Business decisions) لازم تتحدد الأول عشان الـ spec يبقى دقيق:

1. **مصدر الإيراد**: هل هتاخد عمولة من الشركات على كل Lead؟ اشتراك شهري ثابت لكل شركة؟ باقات (Tiers)؟ ولا المحاسبة داخلية بس (مصاريف تشغيل المنصة: استضافة، تسويق، رواتب)؟
2. **مين هيستخدم النظام**: أنت بس (مازن) كأدمن مالي؟ ولا الشركات كمان هتشوف فواتيرها؟
3. **العملة والضرائب**: جنيه مصري بس؟ فيه ضريبة قيمة مضافة لازم تتحسب؟
4. **الدفع**: هل محتاج ربط ببوابة دفع (فوري، باي موب، إنستاباي) ولا التحصيل هيفضل يدوي (تحويل بنكي/كاش) وبس تسجيل الحركة؟

### 8.3 المكوّنات المقترحة (نموذج بيانات أساسي)

```
Account (حساب)          — دليل حسابات: أصول / خصوم / إيرادات / مصاريف / حقوق ملكية
Transaction (حركة)      — قيد مالي (تاريخ، مبلغ، حساب مدين، حساب دائن، وصف، مرجع)
Invoice (فاتورة)        — لو هتصدر فواتير للشركات (عمولة/اشتراك) — رقم، تاريخ، بنود، حالة (مستحقة/مدفوعة/متأخرة)
Payment (دفعة)          — تحصيل فعلي مرتبط بفاتورة أو حركة
Expense (مصروف)         — مصاريف تشغيل المنصة (استضافة، تسويق، رواتب...) بتصنيف
Subscription/Plan       — لو في باقات اشتراك للشركات (اختياري حسب نموذج الإيراد)
Report                  — تقارير جاهزة: أرباح وخسائر، ميزان مراجعة، تدفقات نقدية، مستحقات
AuditLog                — نفس فكرة الموجودة بالفعل في العاصمة — كل حركة مالية مسجّلة بهوية من عملها
```

### 8.4 الربط المقترح مع بيانات العاصمة الحالية
- كل **Company** في العاصمة تبقى ليها **حساب مالي مرتبط** (Account/Customer)، عشان لو في عمولة على كل Lead، الحركة تتسجل تلقائيًا لما حالة الـ Lead تتغيّر لـ Completed.
- استخدام نفس نظام الأدوار (User: ADMIN/PROVIDER) بدل ما تتعمل منظومة مستخدمين جديدة من الصفر.
- استخدام نفس الـ Audit Log الموجود.

### 8.5 الشاشات المقترحة (لو Admin-facing)
- لوحة عامة: إجمالي الإيرادات، المصاريف، صافي الربح (شهري/سنوي)
- دفتر الحسابات (Ledger) بحركاته
- فواتير الشركات + حالة التحصيل
- تسجيل مصروف يدوي
- تقارير قابلة للتصدير (PDF/Excel)

### 8.6 التقنية المقترحة
بما إن الـ Stack الحالي Next.js + Prisma + Postgres (Supabase)، أبسط وأسرع طريق هو **إضافة موديولات جديدة على نفس الـ `api/`** بدل بناء نظام منفصل — كده بتستفيد من نفس الـ Auth، الـ Deployment، والـ Database الموجودين، وميبقاش فيه تكرار جهد أو مشاكل تزامن بيانات بين نظامين.

---

## 9) ملاحظة عن الموقع المباشر (alassema.tech)

اتطلب مني كمان إني أزور `alassema.tech` وأجرد الشركات المسجلة فعليًا، لكن مقدرتش أوصل للموقع دلوقتي:
- إضافة Chrome في هذا الجهاز مش متصلة.
- محاولة الوصول المباشر لـ `alassema.tech` (والـ API بتاعه) رجعت فاضية تمامًا حتى على مسارات بسيطة زي `/api/health` — يعني المشكلة على الأرجح في الشبكة/الاتصال من عندي، مش في محتوى الموقع.

لو حابب، أقدر أعمل واحدة من التلاتة:
1. تتأكد إن الموقع شغّال دلوقتي من عندك وتديني رابط أو Export لبيانات الشركات.
2. تركّب/تفعّل إضافة Claude in Chrome وأعيد المحاولة.
3. أقرا البيانات مباشرة من قاعدة بيانات Supabase بتاعة المشروع لو عندي وصول ليها (فيه أدوات Supabase متاحة ليّا لو حبيت أستخدمها).

---

## 10) مصادر داخل المشروع للرجوع ليها

| الملف | المحتوى |
|---|---|
| `api/prisma/schema.prisma` | نموذج البيانات الكامل |
| `docs/architecture/backend-plan.md` | خطة الباك إند التقنية |
| `docs/architecture/backend-build-plan.md` | خطة البناء بالمراحل |
| `docs/deployment/DEPLOY.md` | دليل النشر خطوة بخطوة |
| `app/FRONTEND.md` | تفاصيل الواجهة |
| `CLAUDE.md` | قواعد العمل على الريبو (رفع، قاعدة بيانات، إلخ) |
