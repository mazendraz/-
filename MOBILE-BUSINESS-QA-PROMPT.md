# برومبت فحص تطبيق البروفيدر والأدمن على المحاكي (mobile/business)

> **طريقة الاستخدام:** افتح Claude Code من جذر المشروع `F:\العاصمة` وانسخ كل اللي جوه
> البلوك اللي تحت كرسالة واحدة. البرومبت ده **فحص وتقرير بس** — مفيش تعديل كود
> ومفيش رفع. لو التقرير طلع مشاكل، هنعمل برومبت إصلاح منفصل بعد كده.

---

```
إنت Senior QA Engineer متخصص في React Native / Expo، شغال على مشروع "العاصمة"
(Al Assema). مهمتك النهاردة: تفحص تطبيق الموظفين `mobile/business` (البروفيدر +
الأدمن) على **محاكي أندرويد حقيقي**، تاخد سكرينشوت لكل شاشة، وتقولّي بالدليل:
هل الـ UI فعلًا كويس؟ وهل كل شاشة متربطة بالباك إند صح ولا بتعرض داتا وهمية /
مش بتنادي على حاجة أصلًا؟

═══════════════════════════════════════════════════════════════════════════════
0. قواعد حمراء — ماتكسرهاش مهما حصل
═══════════════════════════════════════════════════════════════════════════════

1. **ماتعدّلش أي ملف كود.** ده فحص، مش إصلاح. الاستثناء الوحيد: ملفات المخرجات
   اللي هتكتبها في `deliverables/_qa-business/`. لو لقيت باج — سجّله في التقرير
   وكمّل. حتى لو الإصلاح سطر واحد.
2. **ماترفعش حاجة.** مفيش `npm run ship`، مفيش `git commit`، مفيش `git push`،
   مفيش branches. الريبو يفضل نضيف.
3. **قاعدة البيانات: لوكال بس.** اقرا `CLAUDE.md` قسم "قاعدة البيانات — لوكال ضد
   إنتاج" قبل أي أمر. قبل ما تشغّل أي seed، اطبع `DATABASE_URL` من `api/.env`
   وتأكد إنه `postgresql://postgres:postgres@localhost:5433/alassema`. لو فيه
   `supabase.com` أو `supabase.co` جواه — **قف فورًا** وقولّي، ماتشغّلش أي حاجة.
4. **ماتشغّلش `eas update` ولا `eas build`** لأي سبب. خالص.
5. **ماتلمسش `mobile/client`** (تطبيق العميل — منشور وشغال على TestFlight) ولا
   `app/` ولا `desktop/`. اقرا منهم براحتك، بس ماتكتبش فيهم.
6. لو ظهر `index.lock` أو `index file corrupt`: `rm -f .git/index.lock` ثم
   `git reset`. وبعدها `git status` للتأكد.

═══════════════════════════════════════════════════════════════════════════════
1. اقرا الأول — قبل أي أمر
═══════════════════════════════════════════════════════════════════════════════

بالترتيب ده، واعمل ملخص لنفسك من كل واحد قبل ما تكمّل:

- `CLAUDE.md` (جذر المشروع) — قواعد التشغيل والداتابيز.
- `docs/architecture/business-app/README.md` — **دي المرجع الأساسي.** فيها قسم
  "Roles and guards" اللي بيقول الحقيقة الأهم في المشروع كله:
  `withRole` بيختبر تساوي صارم، يعني **الأدمن بياخد 403 على كل
  `/api/provider/*`**. أي شاشة أدمن بتنادي على راوت بروفيدر = باج مؤكد.
- `mobile/business/lib/permissions.ts` — ده presentation-only. كل دالة فيه لازم
  يكون وراها guard حقيقي على السيرفر.
- `api/src/lib/middleware/guards.ts` — الـ guards الفعلية.
- `packages/core/src/theme.ts` — توكنز الألوان والتايبوجرافي (Material 3،
  مستخرجة من `app/tailwind.config.js`). دي المرجع لأي حكم على الألوان.
- `packages/mobile-shared/src/` — خصوصًا `api.ts` (الـ HTTP client)،
  `session.ts` (التوكنز)، `rtl.ts`، `maintenance.ts`، `backendHealth.ts`.
- `mobile/business/app/(provider)/_layout.tsx` و `app/(admin)/_layout.tsx` —
  هيكل التابات.

═══════════════════════════════════════════════════════════════════════════════
2. تجهيز البيئة
═══════════════════════════════════════════════════════════════════════════════

نفّذ بالترتيب، ووقّف واسألني لو أي خطوة فشلت مرتين:

**أ. الباك إند + قاعدة بيانات محلية**
```
docker compose -f api/docker-compose.dev.yml up -d
cd api && npm run db:generate && npm run db:migrate
npm run seed              # داتا الديمو
npm run seed:test-users   # حسابات الاختبار — بيطبع الإيميلات والباسوردات
```
حسابات الاختبار من `api/prisma/seed-test-users.ts`:
- ADMIN: `e2e-admin@local.test` / `e2e-admin-pass-2026`
- PROVIDER: `e2e-provider@local.test` / `e2e-provider-pass-2026` (متربط بشركة)

شغّل السيرفر **مع تسجيل اللوج في ملف** — ده اللي هنعتمد عليه في إثبات الربط:
```
npm run dev:api > deliverables/_qa-business/api-server.log 2>&1
```
(شغّله في ترمنال منفصل / background، وخلي الملف ده مفتوح طول الفحص. Next بيطبع
سطر لكل ريكوست: `GET /api/v1/admin/stats 200 in 45ms` — ده مصدر الحقيقة عندنا.)

اتأكد إن السيرفر سامع على الشبكة مش localhost بس، وإن فايروول ويندوز مش قافل
بورت 3000. اختبر من الترمنال: `curl http://<LAN-IP>:3000/api/v1/app-version`.

**ب. المحاكي**
```
adb devices                    # لو مفيش جهاز:
emulator -list-avds
emulator -avd <اسم الـ AVD> &  # استنى لحد ما adb devices يقول device
```
لو مفيش أي AVD متعرّف، قولّي وقف — ماتنزّلش system images من نفسك.

**ج. التطبيق (dev build — مش Expo Go)**
`mobile/business` مفيهاش مجلد `android/` لسه، يعني `expo run:android` هيعمل
prebuild أول مرة. ده متوقع وماشي.
```
cd mobile/business
npm run sync-lan-ip     # مهم: expo run:android مش بيشغّل الـ pre hook لوحده
cat .env                # تأكد إن EXPO_PUBLIC_API_URL هو http://<LAN-IP>:3000/api/v1
npx expo run:android
```
**قبل ما تكمّل، تأكد بعينك إن `.env` مأشّر على اللوكال مش على
`https://al-assema.tech`.** لو مأشّر على الإنتاج — قف وقولّي.
لو الشاشة طلعت فاضية أو بتقول مفيش اتصال، شغّل `npx expo start --clear` الأول
(كاش Metro بيخزّن قيم `EXPO_PUBLIC_*` القديمة — الحكاية كاملة في `CLAUDE.md`).

**د. مجلد المخرجات**
```
mkdir -p deliverables/_qa-business/screens
```
ماتضيفش السكرينات على git.

═══════════════════════════════════════════════════════════════════════════════
3. أدواتك على المحاكي
═══════════════════════════════════════════════════════════════════════════════

**سكرينشوت** (استخدم الطريقة دي بالظبط — `exec-out` بيبوّظ الملف على ويندوز):
```
adb shell screencap -p /sdcard/s.png
adb pull /sdcard/s.png deliverables/_qa-business/screens/<role>-<screen>.png
```
وبعدين **افتح الصورة بأداة Read وبصّ عليها فعلًا**. ماتكتبش حكم على شاشة من غير
ما تكون شفت السكرين بتاعها.

**التنقّل** — أسرع طريقة الـ deep link (الـ scheme هو `alassemabiz`، شوف
`app.json` و `lib/deepLinks.ts`):
```
adb shell am start -a android.intent.action.VIEW -d "alassemabiz://company/<id>"
```
ولو محتاج تضغط بإيدك، اجيب الإحداثيات الصح مش بالتخمين:
```
adb shell uiautomator dump /sdcard/ui.xml && adb pull /sdcard/ui.xml
adb shell input tap <x> <y>
adb shell input text "..."          # للكتابة
adb shell input keyevent KEYCODE_BACK
```

**لوج التطبيق:**
```
adb logcat -s ReactNativeJS:V *:E
```
شيل اللوج قبل كل شاشة (`adb logcat -c`) عشان اللي يطلع يبقى بتاع الشاشة دي بس.

═══════════════════════════════════════════════════════════════════════════════
4. طريقة فحص كل شاشة — نفس الروتين، بالحرف
═══════════════════════════════════════════════════════════════════════════════

لكل شاشة في الجدول اللي تحت، اعمل الخطوات السبعة دي بالترتيب:

1. `adb logcat -c` + سجّل عدد السطور الحالي في `api-server.log`.
2. افتح الشاشة (deep link أو ضغط).
3. خد سكرينشوت واحفظه باسم `<role>-<route>.png`، **وافتحه بـ Read**.
4. **اقرا ملف الشاشة** (`app/.../<route>.tsx`) وملف الـ lib اللي بينادي منه
   (`lib/<module>.ts`) — اعرف المفروض تنادي على أنهي endpoint.
5. **اقرا السطور الجديدة في `api-server.log`** — أنهي endpoints اتنادت فعلًا،
   وبأنهي status. قارنها باللي توقعته في الخطوة 4.
6. اقرا `adb logcat` — أي وارنينج أو إيرور من React.
7. سجّل النتيجة في جدول التقرير.

**الحُكم على الربط بالباك إند — 4 حالات، صنّف كل شاشة لواحدة منها:**

| الحالة | المعنى |
|--------|--------|
| ✅ **مربوطة** | نادت على endpoint حقيقي، رجع 200، والداتا اللي على الشاشة هي داتا الرد |
| ⚠️ **مربوطة بمشكلة** | نادت بس رجع 4xx/5xx، أو نادت على راوت غلط للدور، أو جزء من الشاشة مش من الرد |
| 🔴 **مش مربوطة** | مفيش أي ريكوست في اللوج والشاشة عارضة داتا — يعني hardcoded / mock |
| ⬜ **مش متفحصة** | مقدرتش توصلها (اشرح ليه) |

للحالة 🔴 خصوصًا: اعمل grep في ملف الشاشة على مصفوفات ثابتة أو أسماء زي
`mock`, `sample`, `dummy`, `TODO`, `placeholder`، وحطّ رقم السطر في التقرير.

**فحص خاص بالأدوار (مهم جدًا):** لكل شاشة أدمن، تأكد إن الـ endpoints اللي
اتنادت كلها `/api/admin/*` أو `/api/v1/...` عامة — **أي نداء على `/api/provider/*`
من حساب أدمن هيرجع 403 وده باج P0**. والعكس: شاشة بروفيدر بتنادي `/api/admin/*`.

**فحص الـ UI — الحكم على السكرين نفسه:**
- **RTL:** التطبيق عربي بس و RTL مفروض. أي عنصر مقلوب، أيقونة في الجهة الغلط،
  نص متلزّق في الحافة، أو تكست محاذي شمال.
- **الألوان:** قارن اللي شايفه بـ `packages/core/src/theme.ts`. أي لون بره
  التوكنز (خصوصًا primary `#005578`) = ملاحظة.
- **الخط والقراءة:** Cairo/Alexandria محمّلين؟ فيه نص مقصوص أو متراكب أو
  حجمه أصغر من إنه يتقرا؟
- **المساحات والمحاذاة:** كروت مش متساوية، بادينج مش متسق بين الشاشات المتشابهة،
  عناصر ملزوقة في حواف الشاشة أو تحت الـ status bar / الـ notch.
- **الحالات الأربعة:** لكل شاشة فيها ليستة، لازم تشوف: loading (سكيليتون ولا
  سبينر ولا شاشة بيضا؟)، empty (رسالة عربية مفهومة ولا فاضي أبيض؟)، error،
  وdata. `components/ListStates.tsx` هو المفروض المسؤول — اتأكد إنه مستخدم.
- **مناطق اللمس:** أزرار أصغر من 44×44 نقطة.
- **الـ tab bar:** الـ badges (طلبات/رسائل/موافقات) بتظهر برقم صح ولا ثابتة؟

═══════════════════════════════════════════════════════════════════════════════
5. جدول الشاشات — غطّيها كلها
═══════════════════════════════════════════════════════════════════════════════

**قبل تسجيل الدخول**
- `sign-in.tsx` — كمان: اكتب باسورد غلط وشوف رسالة الخطأ (عربية؟ مفهومة؟)
- `index.tsx` — التوجيه حسب الدور

**بحساب البروفيدر** (`e2e-provider@local.test`)
- `(provider)/overview` · `(provider)/leads` · `(provider)/messages` · `(provider)/more`
- `lead/[id]` · `lead/[id]/complete`
- `chat/[id]`
- `offerings` · `offering/[id]`
- `projects` · `availability` · `profile` · `notifications` · `analytics`
- `settings/index` · `settings/notifications`
- `waitlist` · `search`

**بحساب الأدمن** (`e2e-admin@local.test`) — سجّل خروج ودخول تاني
- `(admin)/overview` · `leads` · `approvals` · `messages` · `companies` · `more`
- الموافقات: `approvals/project/[id]` · `approvals/review/[id]` ·
  `approvals/feedback/[id]` · `approvals/change-request/[id]` ·
  `approvals/site-review/[id]` · `approvals/site-review-settings`
- الشركات: `company/new` · `company/[id]/index` · `offerings` ·
  `offering/[offeringId]` · `projects` · `reviews` · `availability` ·
  `status` · `waitlist`
- الكتالوج: `categories` · `category/[id]` · `bundle-rules`
- المنصة: `team/index` · `team/[id]` · `platform-waitlist` · `audit-log` ·
  `sessions` · `content/pages` · `content/email-templates` ·
  `settings/maintenance` · `settings/telegram`
- الـ Control Center: `control/index` · `control/overview` · `control/clients` ·
  `control/providers` · `control/operations` · `control/pricing` ·
  `control/reports` · `control/finance/index` · `control/finance/cash-flow` ·
  `control/finance/transactions/index` · `control/finance/transactions/[id]`
  > ملاحظة: دول وراهم `desktopOnly(permission)` — يعني أدمن **بدون** الـ
  > permission المناسبة المفروض يشوف حالة "مالكش صلاحية" مرتبة، مش شاشة
  > مكسورة ولا 403 خام. اتأكد من ده على حساب الاختبار.

═══════════════════════════════════════════════════════════════════════════════
6. اختبارات حواف — بعد ما تخلّص الشاشات
═══════════════════════════════════════════════════════════════════════════════

1. **أوفلاين:** `adb shell svc data disable` + اقفل الواي فاي من المحاكي. افتح
   شاشتين. المفروض `OfflineScreen.tsx` يظهر، مش كراش ولا شاشة بيضا. رجّع الاتصال.
2. **السيرفر واقع:** اقفل `npm run dev:api` وافتح شاشة. المفروض حالة خطأ مفهومة
   (شوف `backendHealth.ts`). رجّعه.
3. **وضع الصيانة:** من شاشة `settings/maintenance` بحساب الأدمن، فعّل الصيانة
   وشوف `MaintenanceScreen` / `MaintenanceBanner` بيظهروا صح — **وأطفيها تاني**.
4. **جلسة منتهية:** امسح التوكن (`adb shell pm clear com.alassema.business`)
   وافتح التطبيق — المفروض يرجع لشاشة الدخول من غير كراش.
5. **تدوير الشاشة والكيبورد:** افتح شاشة فيها فورم طويل (`company/new`)، افتح
   الكيبورد — العناصر بتتزحلق صح ولا الكيبورد بيغطي الحقول؟
   (`KeyboardLift.tsx` / `FormScroll.tsx` هما المسؤولين.)
6. **الخط العربي في الأرقام والفلوس:** `money.ts` و `phone.ts` — الأرقام
   بتتعرض بالشكل الصح في RTL ولا بتتقلب؟

═══════════════════════════════════════════════════════════════════════════════
7. المخرجات
═══════════════════════════════════════════════════════════════════════════════

اكتب `deliverables/_qa-business/REPORT.md` فيه، بالترتيب:

**أ. ملخص تنفيذي** — 10 سطور بالعربي: التطبيق حالته إيه بصراحة، إيه أخطر 3
حاجات، وهل هو جاهز لعرض على حد ولا لأ. من غير تجميل.

**ب. جدول تغطية** — صف لكل شاشة:
| الشاشة | الدور | السكرين | الـ endpoints اللي اتنادت (من اللوج) | حالة الربط | ملاحظات UI |

**ج. النتائج مرتبة بالخطورة:**
- **P0** — التطبيق مكسور: كراش، شاشة بيضا، 403 على دور، داتا وهمية معروضة
  كأنها حقيقية، أو حاجة بتوصل لقاعدة بيانات الإنتاج.
- **P1** — بيشتغل بس غلط: حالة فاضية ناقصة، رسالة خطأ إنجليزية، RTL مكسور،
  ريكوست بيرجع 4xx.
- **P2** — تحسينات UI: مساحات، ألوان بره التوكنز، أحجام لمس.

كل نتيجة لازم تحتوي على: **الملف + رقم السطر**، **اسم ملف السكرين**، **السطر من
`api-server.log` أو `logcat` اللي بيثبتها**، و**سطر واحد بس عن الإصلاح المقترح**
(اقتراح، مش تنفيذ).

**د. رأيك في الـ UI ككل** — بعد ما شفت كل السكرينات: التطبيق متسق؟ شبه بعضه؟
شبه الويب سايت (`app/`) والتطبيق التاني (`mobile/client`)؟ فيه شاشات باين إنها
اتعملت على عجل؟ اتكلم بصراحة، ده أهم جزء عندي.

**هـ. اللي مقدرتش تفحصه** — وليه.

**قبل ما تسلّم:** ارجع على كل نتيجة P0 وأعد التحقق منها مرة تانية على المحاكي.
أي نتيجة مقدرتش تعيد إنتاجها، شيلها من P0 أو اكتب جنبها "مرة واحدة، مش متأكد".
تقرير فيه P0 وهمي أسوأ من تقرير ناقص.

ابدأ بالمرحلة 0 (القراءة)، وبعدين اعرض عليّ خطة مختصرة بترتيب الشاشات اللي
هتمشي عليه قبل ما تفتح المحاكي.
```
